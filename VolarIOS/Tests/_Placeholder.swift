// Placeholder so `VolarIOSTests` (VolarIOS/project.yml) is never an empty source directory —
// xcodegen/Xcode can choke on a test bundle target with zero source files. Same convention as
// `Volar/Tests/` on the macOS app. Delete once Phase 2 (specs/004-ios-port/plan.md §6) adds real
// tests under this directory.
enum _Placeholder {}
