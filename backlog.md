# Backlog — voci

- [ ] Cân nhắc thêm `.claude/` (đặc biệt `.claude/settings.local.json`) vào `.gitignore` để tránh commit nhầm thông tin nhạy cảm. (2026-07-12 — cảnh báo từ `specify init`)
- [ ] Cân nhắc migrate `docs/voci-voice-task-engine-spec.md` sang flow Spec Kit (constitution/specify) nếu muốn dùng spec-driven đầy đủ. (2026-07-12 — spec doc viết trước khi init Spec Kit)
- [ ] FR-013 (feature 001 nextTask) phần **app/persistence-side**: khi xóa task phải strip `id` của nó khỏi `dependsOn` của mọi task khác trong storage, và **notify user một lần** khi việc này đổi trạng thái blocked của task phụ thuộc. `VociCore` (pure engine) chỉ xử lý phần eligibility (coi archived/deleted là resolved); phần mutate storage + notify thuộc feature persistence/reminder, chưa có task. (2026-07-12 — hoãn vì ngoài scope engine, ghi lại theo phân tích /speckit-analyze N1)
- [ ] T018 (feature 001): viết `VociCore/README.md` mô tả public API, link tới `specs/001-nexttask-engine/contracts/nexttask-api.md`. Agent implement bỏ qua vì nằm ngoài danh sách file cốt lõi. (2026-07-12)
- [ ] Verify `VociCore` trên Mac: `cd VociCore && swift build && swift test` — code viết trên Windows, CHƯA compile/test lần nào. Check đầu tiên nếu lỗi: `Task` trùng tên `_Concurrency.Task`. (2026-07-12)
- [ ] Verify `Voci/` app (Phase 1 foundation) trên Mac: `cd Voci && xcodegen generate && open Voci.xcodeproj`, build target **Voci**. Toàn bộ code viết trên Windows, CHƯA compile lần nào. Rủi ro cao nhất: `VociCore.Task` vs `_Concurrency.Task` (đã fully-qualify `VociCore.Task`/`VociCore.nextTask` trong `TaskItem.swift`/`AppState.swift`/`TaskStore.swift` để tránh đụng độ, nhưng chưa test); `@Environment(AppState.self) var x: AppState?` optional-environment syntax trong `Components.swift` (Observation framework — cú pháp optional form có thể cần Xcode 26 mới hỗ trợ đúng, cần check); `#Predicate<VociTask> { $0.id == id }` trong `TaskStore.swift`. (2026-07-12)
- [ ] `Voci/project.yml` chưa có `Assets.xcassets` (AppIcon/AccentColor) — bị bỏ qua vì không nằm trong danh sách 14 deliverable bắt buộc của Phase 1. Cần thêm khi có icon thật, kèm cập nhật `sources:` trong `project.yml`. (2026-07-12)
- [ ] `AppState.confirmSave()` hiện set `deadline: nil` cho task mới tạo từ voice, vì `ParsedTask.when` là `String` hiển thị ("Tomorrow · 2:00 PM"), không phải `Date` — engine `nextTask` sẽ không thấy near-term deadline cho task vừa tạo bằng giọng nói cho tới khi có parser thông minh hơn resolve ra `Date` thật. Tạm chấp nhận cho Phase 1; theo dõi khi nâng cấp `NLParser`. (2026-07-12)
- [x] Focus session countdown (`focusSecondsLeft` đếm ngược mỗi giây) — đã implement trong `Sources/Views/FocusOverlay.swift` (Phase 2, Agent C) bằng `Timer.publish(every: 1...).autoconnect()`. (2026-07-12)
- [ ] `HeuristicNLParser` (`Sources/Model/NLParser.swift`) là parser heuristic đơn giản (NSDataDetector + keyword). Theo đúng spec, đây là "smarter parser is a later swap behind the protocol" — cân nhắc parser tốt hơn (on-device ML) sau khi có usage thật. (2026-07-12)
- [ ] `Sources/Speech/HotkeyManager.swift` (Phase 2, Agent C) đăng ký global hotkey ⌃⌥Space qua `NSEvent.addGlobalMonitorForEvents` — chỉ hoạt động system-wide (khi app khác đang frontmost) nếu user đã cấp quyền **Accessibility** cho Voci trong System Settings → Privacy & Security → Accessibility. Chưa có code request/check quyền này (không có API để chủ động detect trước) và chưa wire vào `AppDelegate`/`VociApp.swift` (để dành Phase 3). Đồng thời `HotkeyManager` dùng `Task { @MainActor in }` bên trong closure của `NSEvent` monitor để thoả Swift 6 strict concurrency — CHƯA build/test trên Mac, đây là điểm rủi ro cao nhất trong file này nếu compiler không chấp nhận cách hop-actor đó. (2026-07-12)
- [ ] `Sources/Audio/AmbientSound.swift` và `Sources/Speech/VoicePlayback.swift`/`HotkeyManager.swift` (Phase 2, Agent C) đều CHƯA compile/test trên Mac — rủi ro cụ thể: `AVAudioUnitEQ`/`AVAudioMixerNode` graph connect trong `AmbientSound`, `AVSpeechUtteranceDefaultSpeechRate` scaling trong `VoicePlayback`, và `.onKeyPress`/`.focusable()` trong `FocusOverlay.swift` (cần Xcode 26 / macOS 26 SDK để chắc API tồn tại đúng như giả định). (2026-07-12)

- [ ] **Phase 3 (Opus, integration) hoàn tất — toàn bộ app Voci macOS (Phase 1–3) đã implement xong nhưng CHƯA build/test lần nào trên Mac.** Đã sửa `Theme.swift` (thêm `Equatable, Hashable` cho `VociAccent`/`Density`/`GlassLevel`), `AppState.swift` (wire `VoicePlayback`/`AmbientSound`/`HotkeyManager`/`SpeechCapture` thật, voice feedback, `toggleAmbientSound()`, `setFrog()`, `activateServices()`), `VociApp.swift` (mount `MenuBarLabel`/`TodayView`/`SettingsView` thật + menu content + `UNUserNotificationCenter` auth), và tạo `Voci/README.md`. Rủi ro cần check đầu tiên khi build trên Mac:
  - `VociCore.Task` vs `_Concurrency.Task` trùng tên (đã fully-qualify ở `TaskItem.swift`/`AppState.swift`/`TaskStore.swift`, nhưng chưa compile-test).
  - `#Predicate` trong `TaskStore.swift` (SwiftData query macro — cú pháp có thể cần điều chỉnh theo Xcode 26 SDK thật).
  - Swift 6 `@Sendable`/`nonisolated(unsafe)` trong `SpeechCapture.swift` (audio tap + recognition-task callback), `HotkeyManager.swift` (`NSEvent` global/local monitor closures), `AmbientSound.swift` (fade-ramp `Task`) — tất cả đều dùng cách hop-actor thủ công, chưa được compiler thật kiểm chứng.
  - `.onKeyPress`/`.focusable()` trong `FocusOverlay.swift` — cần xác nhận API tồn tại đúng như giả định trên macOS 26 SDK.
  - `MenuBarExtra`/`@Environment(\.openWindow)` behavior trong `VociApp.swift` (menu content mới thêm ở Phase 3, dùng `openWindow(id: "main")` từ trong `MenuBarExtra`'s content closure — chưa test có mở đúng `Window("Voci", id: "main")` hay không).
  - `AppState.activateServices()` gọi `hotkey.start(appState:onKeyUp:)` mà **không** truyền `onKeyDown` (vì `HotkeyManager.start` đã tự gọi `appState.startCapture()` trực tiếp trên key-down rồi — truyền thêm `onKeyDown` gọi `startCapture()` lần nữa sẽ double-call). Cần xác nhận hành vi này đúng ý khi test thật trên Mac.

  Follow-up chưa làm (ngoài scope Phase 3 "glue only"):
  - `NLParser` thông minh hơn, resolve `ParsedTask.when` (String hiển thị) thành `Date` thật cho `deadline` — hiện `confirmSave()` vẫn set `deadline: nil` cho task tạo bằng giọng nói (xem mục cũ bên trên).
  - Ambient sound polish thật (âm thanh hiện tại chỉ là white/brown noise tổng hợp đơn giản, chưa tinh chỉnh theo từng mode rain/snow/embers).
  - [x] `MorningFrogView`, `TaskBreakdownView`, `NotificationView` — **đã mount vào `VociApp.swift`/`TodayView.swift`** (2026-07-12, follow-up, CHƯA build/test trên Mac):
    - `MorningFrogView` = daily first-launch sheet: `.sheet(isPresented: appState.showMorningFrog)` trên `Window("Voci", id: "main")`, gated once/day qua `@AppStorage("morningFrogLastShown")` (so với ngày hôm nay, chỉ show khi đã onboard xong và `openTasks` không rỗng). Pick candidate → `AppState.pickFrog(_:)` (gọi `setFrog` + đóng sheet); Skip/voice-answer → `AppState.dismissMorningFrog()`.
    - `TaskBreakdownView` = context-menu → sheet: `TaskRow` có `.contextMenu` mới ("Break down into steps…" → `appState.showBreakdown = true`, cộng "Mark done"/"Delete" tiện thể). "Save all as tasks" → `AppState.saveBreakdown(_:)` (persist sample step titles thành `TaskItem` thật, `.medium`/`.later`, không deadline/duration); Cancel/Edit → đóng sheet. Lưu ý: sheet hiện KHÔNG nhận biết task nào được click — breakdown luôn hiển thị sample "Launch landing page" cố định, chưa wire theo task thật (theo đúng phạm vi yêu cầu của lần mount này).
    - `NotificationView` = in-app banner: parameterized (`title`/`timing`/`onDone`/`onSnooze`/`onReschedule`), hiển thị qua `TodayView`'s `.overlay(alignment: .topTrailing)` khi `appState.reminderBanner != nil`, tự dismiss sau 5s (`Task.sleep`) hoặc khi bấm nút. Trigger thủ công qua menu-bar "Preview reminder" → `AppState.showReminderPreview()`. **Lịch notification thật qua `UNUserNotificationCenter` (deliver theo task deadline) vẫn CHƯA làm** — vẫn chờ `NLParser` resolve `deadline: Date` thật từ giọng nói trước (xem mục cũ ở trên); hiện `AppDelegate` chỉ xin quyền, chưa từng schedule request nào.
  - `TodayView.swift`'s ambient-sound toolbar button vẫn dùng `@State private var ambientSoundPlaying` cục bộ (comment ghi rõ là "UI-only placeholder") thay vì gọi `appState.toggleAmbientSound()`/đọc `appState.ambientSound.isPlaying` thật — `toggleAmbientSound()` đã có ở `AppState` từ Phase 3 nhưng chưa được `TodayView` dùng (nằm ngoài phạm vi sửa đổi cho phép của Phase 3 — chỉ được sửa `Theme.swift`/`AppState.swift`/`VociApp.swift`). (2026-07-12)

- [x] Custom app background giờ đã user-controllable: **Settings → Appearance → Background** (segmented None/Rain/Snow/Fireflies/Custom, bind `appState.ambient` qua `setAmbient(_:)`) + row "Custom image" (chỉ hiện khi mode = Custom) với `NSOpenPanel` file picker (`chooseImage`) và nút Remove. Persist qua `UserDefaults` (`voci.ambient`/`voci.customImageURL`), đọc lại trong `AppState.init`. Rendering (`AmbientBackground`/`CustomImageLayer`/glass panels trong `TodayView`) đã có sẵn từ trước, chỉ thiếu control UI — đã thêm. Đổi `AmbientMode` sang `String`-backed + `CaseIterable`/`Identifiable` để bind trực tiếp vào `Segmented<AmbientMode>`. App không sandbox nên dùng raw file path (`url.path`) — nếu sau này bật App Sandbox thì `NSOpenPanel` result cần security-scoped bookmark thay vì path thô (đã ghi chú trong code). CHƯA build/test trên Mac — mã viết trên Windows theo quy tắc split-env. (2026-07-12)
