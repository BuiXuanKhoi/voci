# Backlog - voci

- [ ] **★★★★ PARITY AUDIT macOS ↔ Windows 2026-07-26 (Opus điều 2 agent kiểm kê read-only cùng schema, rồi tự diff). Kết luận: parity FEATURE tốt, lệch nặng ở 2 chỗ.**
  - **✅ Điểm tốt nhất:** cả 2 agent đều KHÔNG tìm thấy task nào tick `[x]` mà không có code. 46/27 khớp thật, không phải khớp trên giấy. US6 (T045-T050) cả hai nền tảng đều chưa làm (breakdown vẫn stub, `Save all` disabled cả hai; PanicView không tồn tại cả hai) — đúng parity, không phải drift.
  - **🔴 LỆCH CẤU TRÚC — ĐÃ SỬA:** `supabase/` tồn tại ở CẢ HAI nhánh và đã phân kỳ. Nhánh `window` thiếu `subscription/`, thiếu migration 0002, còn App Attest. Đã sync (`window` commit `85679cd`). **BẪY CÒN LẠI: chỉ có MỘT project Supabase nhưng backend được version ở 2 nhánh — deploy từ nhánh sai sẽ ghi đè ngược và làm chết cả 2 client. Kiểm nhánh trước mọi lần deploy.**
  - **Lệch do giới hạn nền tảng (chấp nhận, không sửa được):** (a) macOS có live caption khi nói (Apple streaming), Windows batch-only — Windows không có OS streaming STT; (b) Sign in with Apple chỉ có trên macOS; (c) mua Pro chỉ có trên macOS (app Windows unpackaged → không dùng được Store IAP). (c) không chặn user vì entitlement theo account: mua trên Mac thì Windows tự động Pro.
  - **✅ QUYẾT ĐỊNH anh Khôi 2026-07-26: Windows dựa hẳn vào CLOUD PARSE**, không đầu tư ONNX SLM on-device. `OnnxSlmParser` để nguyên dạng stub mặc định TẮT — **đây là quyết định PARK, không phải việc dở**. Router Windows thực tế = Cloud → Heuristic (đã verify đúng, không cần sửa code). Hệ quả cần biết: **user Windows chưa đăng nhập / chưa opt-in cloud chỉ có parse heuristic thô** — yếu hơn macOS 26 (có FoundationModels on-device). Cân nhắc làm rõ giá trị cloud parse trong onboarding Windows (20 lượt/ngày miễn phí). **KHÔNG được bật cloud parse mặc định** — vi phạm Principle I (opt-in) + sai privacy label.
  - **Lệch do thiếu sót (sửa được, chưa làm) — Windows thiếu so với macOS:** ambient background là gradient tĩnh (macOS có Canvas particle thật); banner nhắc trong app là preview-only (toast không bắn event "delivered" để UI bắt); **không có picker chọn ngôn ngữ nhận giọng** (macOS có + sentinel "auto"); tray không có badge "⏳ N chờ AI" (macOS có trên menu bar).
  - **Lệch độ tin cậy — ngược chiều nhau:** Windows build sạch + **1165/1165 test xanh** + chạy exe thật; macOS **chưa từng compile lần nào**, không có cả `.xcodeproj`, `swift test` VolarCore chưa từng chạy. Đừng so 2 bản như thể cùng độ chín.

- [ ] **★★ KHÔNG TEST ĐƯỢC MUA PRO — thiếu file `.storekit` (2026-07-26):** repo không có StoreKit Configuration file nào và products chưa tạo trên App Store Connect ⇒ nút mua không load được product (không phải lỗi code). Thêm 1 file `.storekit` local là Xcode giả lập được mua/trial/huỷ ngay trên máy, không cần App Store Connect, không cần sandbox account — và test được cả đường `409 subscription_already_linked`. Chưa làm, chờ anh Khôi duyệt.

- [x] **★★★★ FIX: Groq 403/429 mid-flight giờ degrade êm về on-device (2026-07-26, Sonnet execute, CHƯA build Mac).** Đóng nốt residual gap ghi ở mục "★★★★ ACCOUNT AUTH + STOREKIT CLIENT" ngay dưới (dòng "Residual gap: nếu cached tier... `403 upgrade_required` giữa chừng") — KHÔNG sửa lại text mục đó (giữ nguyên lịch sử), ghi kết quả ở đây.
  - **Đã làm:** `GroqEngine.swift` — catch của `stop()` giờ phân biệt `GroqTranscriptionError.http(403/429, _)` khỏi mọi lỗi khác: bắn qua closure MỚI `onCloudUnavailable: ((URL) async -> Void)?` thay vì `onError` (không bao giờ tới `.error` state nữa cho 2 status này). `AppState.swift` — wire `onCloudUnavailable` trong `startCapture()` (giống pattern `apple.allowServerFallback` đã có), handler mới `handleCloudSpeechUnavailable(session:salvageAudioURL:)`: (1) set flag session-only `groqDegradedThisSession = true` (property mới, KHÔNG đụng `speechEngineChoice` đã persist) → `selectedEngine`'s case `.groq` giờ check thêm `!groqDegradedThisSession`, dừng retry Groq cho các lần capture sau trong cùng phiên chạy app mà không cần chờ network; (2) gọi `Entitlements.shared.refreshStatus()` (affordance có sẵn, không đẻ mechanism song song) để cache tier hết stale; (3) nếu `WhisperKitEngine.isSupported && whisper.isModelReady`, salvage file `.m4a` Groq vừa ghi (chưa bị xoá — `defer` xoá file trong `GroqEngine.stop()` chỉ chạy SAU khi `await onCloudUnavailable` xong) qua method MỚI `WhisperKitEngine.transcribe(audioPath:)` (nhỏ, additive, không đụng `session`/`isRunning`/recorder hiện có) → thành công thì gọi `groq.onFinal?(text)` — TÁI DÙNG đúng closure `startCapture()` đã wire cho phiên đó, nên không bao giờ double-fire `onFinal`; (4) salvage fail/không sẵn sàng → discard êm, `captureState` trả về `.idle` (không phải `.error`, không kẹt mãi ở `.parsing`). Không có UI banner mới — chỉ 1 dòng `print` log giống convention `[Volar.Speech] onError -> ...` đã có (đúng tinh thần "prefer silence over inventing UI": `lastCloudQuotaNote` bên parse cũng chưa có UI đọc).
  - **Verify đã làm (đọc code, KHÔNG sửa):** đường parse (`CloudParser.parseDetailed`, `Volar/Sources/Parsing/CloudParser.swift:184-188`) đã tự degrade êm cho CẢ 403 lẫn 429/5xx/4xx khác — switch statement gộp mọi status ngoài 200/429 vào nhánh `default: return .unavailable`, router (`IntentParsing.swift`) fall through về heuristic không set `lastCloudQuotaNote` (đúng, vì đó chỉ dành cho 429 "gentle note", không phải lỗi/degrade thường). Không cần fix gì bên parse.
  - **Known limitation (chưa fix, chấp nhận được):** `groqDegradedThisSession` KHÔNG tự reset trong phiên chạy — nếu lý do là 429 (hết quota ngày) chứ không phải 403 (tier stale), `Entitlements.refreshStatus()` có thể vẫn trả tier "pro" (quota và tier là 2 field khác nhau trong `SubscriptionStatus`, cache `Entitlements.cachedIsPro` chỉ theo tier) → app sẽ KHÔNG tự thử lại Groq cho tới khi user quit/mở lại app hoặc vào Settings chọn lại engine — đây là hành vi AN TOÀN (không lặp lại round-trip 429 vô ích) nhưng không tự phục hồi ngay khi quota reset lúc nửa đêm nếu user không tương tác gì với Settings. Nếu muốn tự phục hồi giữa phiên, cần thêm field quota vào `Entitlements`'s cached state (hiện chỉ cache tier) — ngoài phạm vi task này (`Entitlements.swift` là file bị cấm sửa).
  - **VERIFY-ON-MAC:** build xanh (chú ý closure `async` mới `GroqEngine.onCloudUnavailable`/`WhisperKitEngine.transcribe(audioPath:)` — cả 2 đều `@MainActor`, không có isolation mới lạ); test thủ công: giả 403/429 (chỉnh proxy hoặc mock) khi đang chọn Groq → xác nhận KHÔNG vào `.error`, xác nhận capture sau đó tự dùng WhisperKit/Apple; nếu WhisperKit đang `.ready` lúc 403/429 xảy ra → xác nhận transcript salvage lên đúng confirm card (không mất câu nói). (2026-07-26)

- [ ] **★★★★ ACCOUNT AUTH + STOREKIT CLIENT (2026-07-26, Sonnet execute theo contract account-auth.md đã chốt; CHƯA build Mac):** Implement `AccountService`/`Entitlements`/`KeychainStore` (Supabase Auth hand-rolled qua URLSession + StoreKit 2), wire lại `ConfigParseCredentialProvider`/`GroqTranscriptionClient` sang account bearer token, thêm tab "Account" trong `SettingsView`. Files mới: `Volar/Sources/Account/{AccountService,KeychainStore,AccountModels}.swift`, `Volar/Sources/Model/Entitlements.swift`. Files sửa: `ConfigParseCredentialProvider.swift`, `GroqTranscriptionClient.swift`, `SettingsView.swift`, `AppState.swift`.
  - **CẦN ANH KHÔI LÀM THỦ CÔNG (ngoài phạm vi 8 file được giao):**
    - Xác nhận `Volar.entitlements` đã có `com.apple.developer.applesignin` (đã thấy trong working tree lúc bắt đầu task này — có vẻ đã làm sẵn, chỉ cần double-check trong Xcode ▸ Signing & Capabilities bật "Sign in with Apple" thật sự cho target).
    - `Volar/project.yml` KHÔNG khai báo tường minh `StoreKit.framework`/`AuthenticationServices.framework` trong `dependencies:` — cả hai là system framework nên thường tự link được khi `import`, nhưng nếu `xcodegen generate && xcodebuild` báo lỗi thiếu symbol thì thêm `- sdk: StoreKit.framework` + `- sdk: AuthenticationServices.framework` vào target `Volar`.
    - App Store Connect: tạo 2 product `tech.kioh.Volar.pro.monthly` ($6.99/mo) + `tech.kioh.Volar.pro.yearly` ($49.99/yr), 1 subscription group "Volar Pro", intro offer 14-day free trial trên cả 2 — xem `docs/app-store-connect-checklist.md` nếu agent khác đã tạo file đó.
  - **Known gaps tự flag (self-review, CHƯA fix — nằm ngoài 8 file được giao hoặc cần build Mac để verify):**
    - `AppleSignInCoordinator` (trong `AccountService.swift`) đánh dấu `@MainActor` dựa trên giả định `ASAuthorizationControllerDelegate` callback chạy trên main thread (giống pattern `SpeechCapture.ContinuationOnce` đã ghi chú) — **UNVERIFIED**, chưa chạy thử trên Mac thật. Nếu trap `EXC_BREAKPOINT` khi bấm "Sign in with Apple", sửa theo đúng kỹ thuật `SpeechCapture.swift` đã dùng (delegate method `nonisolated`, chỉ trích Sendable primitive rồi hop `@MainActor` bằng `Task`).
    - `GroqTranscriptionClient.swift`: đã XOÁ hoàn toàn cơ chế dev-token (`GROQ_PROXY_TOKEN`/`GROQ_API_KEY`/`volar.groqToken`) để smoke-test Groq trực tiếp không cần account — nếu cần lại escape hatch cho dev, phải thiết kế lại (không phục hồi cơ chế cũ, theo đúng tinh thần xoá `PARSE_DEV_TOKEN` của contract).
    - Residual gap: nếu cached tier (`Entitlements.cachedIsPro`, UserDefaults snapshot) bị lag so với thực tế (vd subscription vừa hết hạn, `refreshStatus()` chưa kịp chạy lại), request Groq vẫn có thể bay tới server và nhận `403 upgrade_required` giữa chừng — `GroqEngine.swift` (KHÔNG nằm trong 8 file được giao) là nơi quyết định lỗi đó hiển thị ra sao (hiện tại: `onError`, capture báo lỗi thay vì im lặng fallback). Muốn fix triệt để cần sửa `GroqEngine.swift`.
    - `VolarIconName` (`Design/VolarIcon.swift`, không nằm trong 8 file được giao) không có icon "person/account" — tab Account tạm dùng `.check` (checkmark) làm icon thay thế. Nên thêm icon riêng khi ai đó có quyền sửa file đó.
    - Chưa build/test trên Mac — mọi assumption về Swift 6 concurrency (actor isolation, `@MainActor` inference) trong `AccountService.swift`/`Entitlements.swift` là UNVERIFIED cho tới khi build xanh.
  - **VERIFY-ON-MAC:** build xanh; Sign in with Apple thật (kiểm tra email chỉ đọc từ `session.user.email`, không phải credential); email OTP end-to-end; purchase sandbox 2 product + xác nhận `/subscription/link` gọi đúng thứ tự (link trước `transaction.finish()`); restore purchases; 409 `subscription_already_linked` khi gắn cùng 1 subscription cho 2 account; delete account xoá sạch Keychain; Groq/parse fallback im lặng khi signed-out/free-tier/offline/quota hết.



- [ ] **★★★★★ SECURITY: dev-token bypass cho edge function `/parse` + `/groq` (2026-07-26, Sonnet execute theo design Opus đã chốt, CHƯA build Mac, CHƯA deploy):** Không có StoreKit (Phase 8) nên trước đây KHÔNG có cách nào gọi được `/parse`/`/groq` để tự test (mọi request 401). Thêm bypass đọc secret `PARSE_DEV_TOKEN` vào `verifyPaidAuth` (`supabase/functions/_shared/auth.ts`, hàm dùng chung cho cả 2 route) qua helper `tryDevTokenBypass`.
  - **Thiết kế:** bypass CHỈ tồn tại khi secret `PARSE_DEV_TOKEN` được set trong Supabase; secret ngắn hơn 32 ký tự bị coi như CHƯA set (log `auth_dev_token_too_weak`, rơi xuống đường StoreKit bình thường). So sánh constant-time: SHA-256 cả token trình lên lẫn secret cấu hình → hàm tự viết `constantTimeHexEqual` (XOR-accumulate trên 2 chuỗi hex digest 32-byte cố định, không return sớm giữa vòng lặp) — **KHÔNG dùng `node:crypto`'s `timingSafeEqual`**: `auth.ts` là top-level import của CẢ `/parse` LẪN `/groq`, nếu shim `node:crypto` trên Supabase Edge Runtime không expose đúng API đó thì cả hai function chết load hoàn toàn (mọi request 500, kể cả đường StoreKit không liên quan) — rủi ro không đáng đánh đổi cho một hàm so sánh 5 dòng tự viết được. Khớp → trả `{ mode: "paid", rateLimitKeyHash: sha256Hex("devtoken:" + sha256Hex(token)) }` — mọi caller dev-token dùng CHUNG một quota key qua `checkPaidRateLimit`, không nhân bản rate limit. Log `auth_dev_token_used` khi có traffic qua đường này (không log token). Bypass chạy SAU kiểm tra hình dạng header (`Bearer ` prefix, độ dài) nhưng TRƯỚC `hasPinnedAlg` (dev token không phải JWS) và TRƯỚC `requireEnv(APPSTORE_ENV_NAMES)` (StoreKit env chưa hề set → sẽ 503 trước khi tới bypass nếu đặt sau).
  - **🔴🔴🔴 BẮT BUỘC XOÁ SECRET `PARSE_DEV_TOKEN` TRƯỚC KHI PHÁT HÀNH CHO NGƯỜI DÙNG THẬT.** (`supabase secrets unset PARSE_DEV_TOKEN`) — xoá secret là đủ, không cần sửa code, bypass biến mất khỏi runtime ngay. Token dev KHÔNG BAO GIỜ được nhúng vào bản build gửi cho người khác test.
  - **⚠️ CẦN REDEPLOY cả 2 function để có hiệu lực:** `supabase functions deploy parse` + `supabase functions deploy groq` (đổi code `_shared/auth.ts` dùng chung, cả 2 route đều cần deploy lại).
  - **VERIFY:** (1) set secret `PARSE_DEV_TOKEN` (≥32 ký tự ngẫu nhiên) qua Supabase Dashboard/CLI, KHÔNG commit vào repo; (2) gọi `/parse` và `/groq` với `Authorization: Bearer <token đó>` → phải qua auth (mode paid); (3) thử token sai / token ngắn <32 ký tự → phải rơi về 401 như cũ (không bypass); (4) xác nhận log Supabase thấy event `auth_dev_token_used` khi dùng token đúng, không thấy token nào bị log ở đâu; (5) build Swift/Deno type-check chưa chạy được trên Windows — verify cú pháp TypeScript khi deploy thật.
  - **Cùng đợt — default base URL (bỏ bắt user gõ tay UserDefaults):** `Volar/Sources/Parsing/ConfigParseCredentialProvider.swift` — `baseURL()` giờ default `https://nuzrpipwacravfgsiacv.supabase.co` (override vẫn qua `volar.parseProxyBaseURL`), `isConfigured` giờ chỉ phụ thuộc token (base URL luôn có). `Volar/Sources/Speech/GroqTranscriptionClient.swift` — `EnvironmentGroqCredentialProvider.baseURL()` default đổi từ gọi thẳng Groq sang proxy `https://nuzrpipwacravfgsiacv.supabase.co/functions/v1/groq` (client tự append `audio/transcriptions` → ra đúng `.../functions/v1/groq/audio/transcriptions` khớp edge function); hằng Groq trực tiếp đổi tên `groqDirectBaseURLForSmokeTesting`, vẫn dùng được qua UserDefaults `volar.groqBaseURL` khi cần smoke-test thẳng Groq. **VERIFY-ON-MAC:** build xanh; xác nhận Settings status row (Cloud parsing / Groq) không nói dối khi chỉ thiếu token (base URL giờ luôn "có sẵn").
  - Sửa thêm 1 comment sai ở `Volar/Sources/App/AppState.swift` (~dòng 756): nói đúng lại WhisperKit KHÔNG nhận locale/language (tự auto-detect), chỉ `speech` (Apple) và `groq` được `setRecognitionLocale` áp dụng.

- [ ] **★★★ FIX: Groq nghe tiếng Việt sai bét (auto-detect kém với clip ngắn) + wire Recognition-language picker vào Groq (2026-07-26, Sonnet execute theo design đã chốt, CHƯA build Mac):** Nguyên nhân gốc: client cố ý không gửi `language` cho Groq để auto-detect, nhưng Whisper auto-detect suy luận trên cửa sổ 30s đầu (đa phần silence padding với clip 2-5s) nên hay đoán nhầm sang tiếng Anh. Phát hiện thêm: `AppState.swift:436` hardcode default `recognitionLocaleID = "en-US"`, bỏ qua ngôn ngữ hệ thống.
  - **Đã làm:** sentinel `"auto"` cho `recognitionLocaleID` (`AppState.autoRecognitionLocaleID`) — Apple engine (`SpeechCapture.setLocale`) dùng `Locale.current` khi "auto", locale cụ thể giữ nguyên hành vi cũ; Groq (`GroqEngine.languageCode`, mới) nhận mã ISO-639-1 2 ký tự (`Locale(identifier:).language.languageCode?.identifier`) khi có locale cụ thể, `nil` khi "auto" → `GroqTranscriptionClient.transcribe` chỉ gửi field `language` khi non-nil. Default đổi `"en-US"` → `"auto"`. Picker `SettingsView.generalTab` thêm "Automatic (multilingual)" tag `"auto"` ở đầu danh sách. Edge function `supabase/functions/groq/index.ts` đổi từ strip `language` vô điều kiện → validate `/^[a-z]{2}$/` trước khi forward (defense-in-depth, input untrusted từ client). Sửa hết comment/doc drift liên quan ("no language on purpose") ở `GroqTranscriptionClient.swift`/`GroqEngine.swift`/`index.ts`/README.md.
  - **⚠️ CẦN REDEPLOY edge function `groq`** (`supabase functions deploy groq`) — code server-side đổi thì phải deploy lại mới có hiệu lực, thay đổi client-side không tự động áp dụng cho phía server.
  - **✅ FIX REGRESSION (Opus review phát hiện, cùng ngày):** default `"auto"` resolve thành `Locale.current` phía Apple engine → `SFSpeechRecognizer(locale:)` trả `nil` nếu ngôn ngữ hệ thống không nằm đúng trong `supportedLocales()` (region lạ, locale kiểu "en-VN", hoặc locale có extension như "vi-VN@calendar=gregorian") → capture chết ngay lần chạy đầu. Sửa: `SpeechCapture.swift` thêm `resolvedRecognizer(for:)` (dùng chung cho cả `init` lẫn `setLocale`) — thử locale chính xác trước, không được thì tìm locale khác trong `supportedLocales()` cùng `language.languageCode`, cuối cùng fallback `"en-US"` (hành vi cũ, không thể tệ hơn trước). Có log `print("[Volar.Speech] ...")` cho biết đã fallback sang locale nào.
  - **VERIFY-ON-MAC:** (1) build xanh (Swift 6 concurrency: `GroqEngine.languageCode` là stored property trong class `@MainActor`, không cần `nonisolated` vì chỉ đọc/ghi từ context `@MainActor` khác; `SpeechCapture.resolvedRecognizer` cũng vậy — private static, cùng isolation MainActor với `init`/`setLocale` gọi nó); (2) thử nói tiếng Việt với Apple engine, chọn locale `vi-VN` cụ thể VÀ để "Automatic"; (3) thử Groq (cần key) với locale cụ thể (mã "vi" phải lên đúng) và với "Automatic" (vẫn auto-detect như cũ); (4) xác nhận user đã persist locale cũ (vd đã từng chọn "en-US" tường minh trước đây) không bị đổi hành vi — chỉ user CHƯA từng chọn gì (default) mới đổi từ "en-US" cứng sang "auto"; (5) **QUAN TRỌNG:** thử chọn "Automatic" với vài ngôn ngữ hệ thống (macOS System Settings ▸ Language & Region) khác nhau — kể cả ngôn ngữ ít phổ biến / region lạ — xác nhận capture KHÔNG chết (không rơi vào `.recognizerUnavailable`), theo dõi log `[Volar.Speech]` để biết có fallback hay không.

- [ ] **★★ UX: không có lối vào Settings từ cửa sổ chính (2026-07-26):** TodayView không có nút/icon bánh răng nào mở Settings — đường vào duy nhất là menu-bar dropdown "Settings…" (SettingsLink) hoặc ⌘, khi window Volar đang key. Anh Khôi mở app xong không tìm thấy Settings (phát hiện khi verify trên Mac). Đề xuất (CHƯA duyệt): thêm icon gear ở TodayView (header/sidebar) gọi `SettingsLink`/`openSettings`. Kèm theo: kiểm tra bẫy LSUIElement — bấm "Settings…" từ MenuBarExtra có thể mở cửa sổ Settings PHÍA SAU app khác vì app không tự activate; nếu dính thì thêm `NSApp.activate(ignoringOtherApps: true)` khi mở Settings.

- [ ] **★★★ SETTINGS: Local↔Cloud parse switch 2026-07-22 (Opus design + Sonnet execute + Opus review; CHƯA build Mac):** Thêm picker "Task parsing" trong Settings → General cho user chọn `On-device (private, free)` vs `Cloud AI (better quality)`. Thiết kế tái dùng `cloudParseConsent`/`cloudParseConsentKey` làm công tắc (gate cloud tier sẵn có qua `DefaultCloudParseGate.isOptedIn()`) — không đẻ key mới, không đụng PopoverView/gate. Files: **new** `Parsing/ConfigParseCredentialProvider.swift` (stub credential đọc UserDefaults `volar.parseProxyBaseURL`/`volar.parseProxyToken`), `AppState.swift` (enum `ParseEnginePreference`, getter `parseEnginePreference`, setter `setParseEngine`, wire `cloud:` vào router default L405), `SettingsView.swift` (picker + status row). **CÒN LÀM:**
  - [x] **API key/backend:** SUPERSEDED 2026-07-26 — `ConfigParseCredentialProvider` không còn đọc `volar.parseProxyToken` từ UserDefaults; giờ lấy account access token qua `AccountService.shared.validAccessToken()` (xem mục "★★★★ ACCOUNT AUTH + STOREKIT CLIENT" đầu file).
  - [x] **Phase 8/T051:** SUPERSEDED 2026-07-26 — composite thật giờ là `AccountService` (Supabase Auth) chứ không phải StoreKit-JWS-làm-bearer + `DeviceCheckProvider`; `DeviceCheckProvider.swift`/App Attest bị bỏ hoàn toàn theo contract account-auth.md.
  - [ ] **VERIFY-ON-MAC:** build xanh + thử switch picker + xác nhận cloud unconfigured → im lặng fallback on-device, không crash. (2026-07-22)
  - **★ CÙNG ĐỢT — Speech engine (AI nghe) cũng switch local↔cloud (2026-07-22):** Picker "Speech engine" (Apple/WhisperKit/Groq) đã có sẵn + wire thật; sửa cho nhất quán với parse: Groq (cloud) khi CHƯA có key giờ **fallback êm về Apple on-device** thay vì lỗi cứng `.missingCredentials` lúc upload. Thêm `EnvironmentGroqCredentialProvider.isConfigured` + `GroqEngine.isConfigured` (đọc env `GROQ_PROXY_TOKEN`/`GROQ_API_KEY` hoặc UserDefaults `volar.groqToken`), `selectedEngine` case `.groq` gate qua đó (`GroqTranscriptionClient.swift`, `GroqEngine.swift`, `AppState.swift:678`), + status row trong Settings. **Key đưa sau:** set `volar.groqToken` (hoặc env) thì Groq transcribe thật. **Production còn thiếu:** ~~provider proxy/StoreKit/Keychain thật thay `EnvironmentGroqCredentialProvider` dev-only~~ — SUPERSEDED 2026-07-26: `EnvironmentGroqCredentialProvider` giờ Keychain+Entitlements-backed thật (signed-in + cached Pro tier), env-token dev bypass đã bị xoá hoàn toàn (xem mục "★★★★ ACCOUNT AUTH + STOREKIT CLIENT" đầu file).

- [x] **★★★ QUYẾT ĐỊNH KIẾN TRÚC: có nên bắt user login SSO (Google/Apple) không — ĐÃ CHỐT (2026-07-26):** Trả lời: CÓ — `specs/002-workflow-command-center/contracts/account-auth.md` chốt Supabase Auth (Sign in with Apple + email OTP) làm lớp danh tính duy nhất, thay hoàn toàn App Attest + StoreKit-JWS-làm-bearer. Client-side đã implement (xem mục "★★★★ ACCOUNT AUTH + STOREKIT CLIENT" đầu file này). Giữ nguyên phần phân tích gốc bên dưới làm bối cảnh lịch sử.
  - **Đã phân tích (ghi lại để khỏi nghĩ lại từ đầu):** Login KHÔNG giải quyết mối lo "user bắt request rồi đem token xài cho app khác" — user hợp pháp luôn lạm dụng được credential của chính mình, chỉ rate limit/quota chặn được (auth chống người lạ, quota chống chính chủ). **Paid tier KHÔNG cần login:** `verifyPaidAuth` lấy `originalTransactionId` từ StoreKit JWS (Apple ký, không giả được), quota key = `sha256(bundleId:originalTransactionId)`, ổn định qua gia hạn/cài lại/đổi máy cùng Apple ID, revoke được nhờ `enableOnlineChecks`, rate limit 20 req/phút qua `PARSE_PAID_RPM` — tức quota key ≠ tài khoản, chỉ cần định danh ổn định + không giả mạo được. **Chỗ thủng thật là FREE tier:** `checkFreeQuota` cần `quotaKeyHash` từ App Attest keyId, mà App Attest gần như không dùng được trên macOS → `verifyFreeAuth` fail-closed 503 (chi tiết + 3 lựa chọn xử lý xem mục ★★★★ FULL-SOURCE REVIEW 2026-07-18 bên dưới, câu hỏi 2 — hai mục này trỏ chéo nhau, không chép lại nội dung). **Cái giá của login với app này:** Volar bán bằng "bấm hotkey, nói, xong" — bắt đăng nhập trước câu nói đầu tiên là giết wedge (nhất là tệp ADHD); kèm PII/GDPR, guideline 5.1.1(v) (nút xoá tài khoản), guideline 4.8 (bắt buộc Sign in with Apple nếu đã có Google/Facebook); mâu thuẫn positioning privacy-first. **Lý do DUY NHẤT login thực sự đáng:** bản Windows không có StoreKit — nếu định bán thuê bao chung Mac+Windows một gói thì buộc phải có account (Stripe + SSO), phải thiết kế từ đầu chứ đừng chắp vá (branch `window` đang hoãn monetization).
  - **CÂU HỎI TREO CẦN ANH KHÔI TRẢ LỜI:** bản Windows có định bán tiền không? Trả lời quyết định luôn kiến trúc auth — có thì làm account ngay từ đầu, không thì giữ không-account (StoreKit cho paid, free về on-device thuần) và `PARSE_DEV_TOKEN` chỉ sống tới khi StoreKit xong.
  - **Khuyến nghị hiện tại (chưa duyệt):** giữ không-account cho bản Mac sắp ship; nếu sau này cần account thì dùng Supabase Auth (đã có sẵn trong project) với Sign in with Apple + Google, bỏ Facebook.

- [ ] **★★★★★ APPSTATE BUG-HUNT 2026-07-19 (Opus triage + 4 Sonnet review agents song song + Opus verify từng finding → 1 Sonnet fix agent → Opus review diff):** Soi `AppState.swift` (2220 dòng) theo 4 vùng disjoint. **4 bug đã fix, CHƯA build Mac:**
  1. `stopCapture()` nuốt transcript khi double-tap (`AppState.swift:757`) — `.parsing` trước đây chỉ set cho batch engine, nên engine Apple (mặc định) kẹt `.recording` trong lúc chờ final async → lần `stopCapture()` thứ 2 bump `captureSession` → `onFinal` bị guard chặn → transcript mất im lặng. Fix: luôn set `.parsing` khi gọi `engine.stop()`.
  2. Chỉ MỘT task `.afterDate` được đăng ký resurface (`AppState.swift:1789`) — vi phạm FR-017. `nextResurfaceDate` trả 1 mốc; local wake không chain; `rebuildFromStorage` chỉ quét `deadline != nil`. Fix: đăng ký durable cho MỌI task có `.afterDate` tương lai + chain lại khi wake fire + re-arm trong `activateServices()`.
  3. `completeFocusTask` đọc sai số task còn lại (`AppState.swift:1565`) — đoán "-1" trước `toggleDone`, sai với recurrence reset-in-place (delta 0) và cascade parent (delta -2) → đọc to sai số. Fix: đếm `openTasks.count` sau mutation.
  4. `accent`/`density` không persist (`AppState.swift:405-410`) — user đổi theme, quit, mở lại là mất. Fix: thêm key + load trong init + `setAccent`/`setDensity` (đụng `SettingsView.swift` 2 call site). `Density` không `RawRepresentable` → map string thủ công mirror `SettingsView.densityID`.
  - **⚠️ PHÁT HIỆN CẦN CHÚ Ý:** test `SnapshotTests.unCompletingReblocksDependentNotInDiff` có assertion `#expect(diff.isEmpty)` **SAI từ đầu** (đúng phải là `diff == [prerequisite.id]`, vì `eligibleTasks` loại task `.done` nên prerequisite reopened = newly eligible). Đã sửa. **Suy ra: suite `VolarCore` chưa từng chạy lần nào** → khi lên Mac phải chạy `swift test` toàn bộ VolarCore, dự kiến còn assertion sai khác tương tự.
  - **Compile fix Swift 6 cùng đợt (2026-07-19):** (a) `AppState.cloudParseConsentKey` → `nonisolated static let` — cùng lớp lỗi với `IntentRouter.maxTaskCap`: `static let` trong kiểu `@MainActor` kế thừa isolation, mà `DefaultCloudParseGate` thì nonisolated; (b) `DefaultCloudParseGate.isOnline()` dùng `lock.withLock {}` thay `lock()`/`defer unlock()` — `NSLock.lock/unlock` là `@available(*, noasync)`, không gọi được trong hàm `async`; (c) **preemptive** `HotkeyManager.carbonEventHandler` → `nonisolated static let` vì Swift 6 không tạo được `@convention(c)` pointer từ closure actor-isolated (chưa gặp lỗi thật, sửa trước theo cùng logic — nếu Mac báo khác thì xem lại).
  - **Bài học lớp lỗi này:** đã quét toàn repo cho 2 pattern — mọi `static` khác trong kiểu `@MainActor` (`TaskStore.maxBatchSize`, `DelegationTracker.waitingPrefix`/`isWaitingOnAI`, `ReminderScheduler.systemRequestCap`, `AppState.voiceDeliveryModeKey`/`globalReminderPolicyKey`) đều chỉ được đọc từ context `@MainActor` khác → cùng actor, không cần sửa. `ContinuationOnce.resume` (SpeechCapture.swift:345) giữ nguyên `lock()`/`unlock()` thủ công vì là hàm đồng bộ, `noasync` không áp dụng.
  - **VERIFY-ON-MAC:** 4 fix trên + 3 compile fix + `swift test` VolarCore đầy đủ. (2026-07-19)

- [ ] **★★★★ FULL-SOURCE REVIEW 2026-07-18 (Fable review toàn repo → 6 Sonnet fix agents; supersedes phần "VERIFY HẾT" của ★★★):** Review tĩnh 6 subsystem (Speech/Parsing/Reminders+Model/Views/Orchestrator+Core/Supabase). Kết quả: 2 BLOCKER compile + ~15 MAJOR đã giao 6 Sonnet agents fix song song file-disjoint (Reminders, Speech-engines, App-wiring+Views, Orchestrator+TaskStore, Parsing, Supabase). Sau khi fix xong → Fable review diff → commit. Các mục DƯỚI ĐÂY là phần **hoãn lại / cần quyết** từ đợt review này:
  - **❓ CẦN ANH KHÔI QUYẾT (trade-off, chưa làm):**
    1. **Capture UI không hiện khi window đóng** — hotkey ⌃⌥M hoạt động toàn cục nhưng PopoverView (UI confirm duy nhất) chỉ mount trong TodayView → nói xong không thấy gì nếu window đóng. Options: (a) auto-open main window khi hotkey capture, (b) mount PopoverView vào MenuBarExtra window, (c) NSPanel nổi riêng. Em nghiêng (c) NSPanel (đúng chất menu-bar app), nhưng cần anh chốt.
    2. **App Attest gần như KHÔNG hỗ trợ native macOS** + entitlement `appattest-environment` chưa có → free-tier cloud parse chết về cấu trúc trên macOS (degrade an toàn về heuristic, không crash). Cần quyết: đổi scheme free-tier (ví dụ rate-limit theo IP + soft device id) hay chấp nhận cloud parse chỉ cho paid (StoreKit JWS, Phase 8). *(Liên quan: mục ★★★ QUYẾT ĐỊNH KIẾN TRÚC login SSO 2026-07-26 ở trên có phân tích thêm 3 lựa chọn xử lý free tier — login là 1 trong 3, chưa chốt.)*
    3. **Supabase: quota trừ TRƯỚC khi gọi Gemini** — Gemini sập thì user vẫn mất lượt. Refund-on-upstream-failure hay chấp nhận? (MINOR, ảnh hưởng UX free tier.)
    4. **Groq hiện trong Settings nhưng chưa có credential provider** (Phase 8) → user chọn là lỗi `.missingCredentials`. Ẩn/disable tạm hay để nguyên?
  - **Residual từ vòng Fable-review-diff (2026-07-18, nhỏ, không chặn):** (a) `AmbientSound` không phải `@Observable` → icon nút ambient trong TodayView có thể không re-render tức thì khi `isPlaying` đổi (follow-up: mirror cờ vào AppState hoặc cho AmbientSound @Observable); (b) `rebuildFromStorage` giờ chạy phần reconcile/fire trong async hop → `ReminderSchedulerTests` có thể cần expectation/await khi chạy trên Mac; (c) Supabase: các 503 config của nhánh App-Attest (free, đang stub) vẫn trả `detail`/`missingEnv` — chưa opaque như nhánh paid (làm nốt khi free tier sống); (d) `.voiceOnly` chặn cả banner lẫn sound-ding cùng nhau (chấp nhận, ghi để biết); (e) PopoverView `conditionRow` còn param `confidence` không dùng (warning cosmetic).
  - **Hoãn (kỹ thuật, không chặn build):** notification-authorization denied không được surface trong Settings (reminders chết câm nếu user từ chối); `UNTimeIntervalNotificationTrigger` → nên đổi `UNCalendarNotificationTrigger` (lệch khi đổi timezone/clock); `refillSystemRequests` chưa serialize (race vượt cap 60); retention cho `ReminderRecord`/`CompletionEvent`/`ParseCorrection` (phình vô hạn); `isSensitive` chưa có UI set (redaction unreachable); `cwdHint` không bao giờ được populate (matching ladder bước 2 dead — giới hạn v2); `lastCloudQuotaNote`/`lastRoute` chưa có UI đọc (wire khi cloud tier sống); NSDataDetector resolve theo wall-clock thay vì `now` inject (chỉ ảnh hưởng test); Sidebar còn số đếm prototype; TodayView all-done che mất Completed drawer; ReminderSchedulerTests đụng store thật + delegate thật; Supabase 429 paid nên map UX riêng phía client; ClaudeCodeConnector `State` dead code + `tty` không lưu; `dueForRecheck` sort thiếu tiebreak.
  - **VERIFY-ON-MAC bổ sung (thêm vào checklist docs/mac-verify-checklist.md khi build):** WhisperKit 0.9 `transcribe` region-isolation/Sendable (rủi ro compile cao nhất còn lại); HotkeyManager `EventHandlerUPP` closure→C-pointer dưới Swift 6; `ModelConfiguration("VolarReminders")` → đúng file store riêng; `startAccessingSecurityScopedResource` trên URL panel tươi (first-connect Claude Code); `application(_:open:)` nhận `volar://` khi LSUIElement không có window; JSONSerialization round-trip settings.json không phá format của Claude Code; lệnh hook `base64` trong Stop-hook; MenuBarLabel màu có bị flatten template không; VolarTests: thêm `GENERATE_INFOPLIST_FILE: YES` nếu xcodegen không tự sinh + hardened runtime có thể chặn test injection (cần `com.apple.security.cs.disable-library-validation` cho Debug); `ModelContext.fetchCount`; SwiftData lightweight migration các field mới; `DCError` case names trong catch của DeviceCheckProvider; Gemini model id `gemini-3.1-flash-lite` (agent Supabase đang verify bằng web). (2026-07-18)

- [ ] **★★★ PROGRESS 2026-07-17 (TIẾP TỪ ĐÂY — supersedes các mục ★★/★ cũ bên dưới):** Feature 002 gần xong ship-gate P1+P2. Đã push remote. **RENAME: Voci→Volar** (paths `Volar/`, `VolarCore`, tokens `Volar*`, scheme `volar://`; repo path vẫn `C:\projects\voci`). Trạng thái:
  - ✅ **Phase 1–5** code-complete + Opus review + fix + committed + pushed (P1/2 engine+persistence, P3 capture, P3b conflict, P4 reminders+spoken-delivery, P5 voice-done+auto-advance+sweep). TẤT CẢ CHƯA build Mac.
  - ⏳ **Phase 6** (orchestrator US4): subsystem xong commit `a2a066f` (DelegationTracker/AppLinkHandler/ClaudeCodeConnector); **app-wiring agent ĐANG CHẠY** (AppState/VolarApp/MenuBarLabel/TodayView/PopoverView/SettingsView). Xong → Opus review → fix → đóng.
  - ✅ **Studio Dark restyle**: Theme tokens + MenuBarLabel + PopoverView + TodayView xong. CHƯA: FocusOverlay/Settings/sheets.
  - ✅ **Design v2 nâng tầm + logo refined** trên Claude Design project `b2f396b8`; logo repo commit `a171134`.
  - ✅ **Supabase**: migration `parse_quota` applied (project nuzrpipwacravfgsiacv, ref). CHỜ anh Khôi: `supabase functions deploy parse` (CLI — config.toml verify_jwt=false sẵn) + set secret `GEMINI_API_KEY`. Advisor WARN có sẵn `rls_auto_enable()` (không phải của mình).
  - **CÒN LẠI:** (a) đóng Phase 6; (b) **VERIFY HẾT** — Opus review chéo toàn P1–6 (nhất quán liên phase sau rename) + tổng hợp checklist Mac build/test; (c) restyle nốt FocusOverlay/Settings/sheets; (d) **BUILD MAC** — rủi ro: Carbon hotkey, SwiftData migration + 2 container, FoundationModels @Generable, nonisolated(unsafe), volar:// onOpenURL, Deno npm imports khi deploy. **Follow-up:** `TaskStore.setDelegation(_:on:)` để DelegationMeta sống trong store thay vì UserDefaults shadow (6a flag). (2026-07-17)

- [ ] **[SUPERSEDED bởi ★★★ ở trên] ★★ PROGRESS 2026-07-16 tối (session limit):** Feature 002 tiến rất xa, TẤT CẢ CHƯA build/test Mac. Commit gần nhất `6f4f116`. Trạng thái:
  - ✅ **Phase 1+2** (setup/engine/persistence) + fixes — commit tới `b7a3372`.
  - ✅ **Phase 3** (US1 voice capture) + Opus review + fix — `dbfdfce`/`54ea5b0`.
  - ✅ **Phase 3b** (conflict advisory FR-011c, ConflictCheck engine) — trong Phase 4 batch.
  - ✅ **Phase 4** (US2 reminders + spoken delivery FR-014b + conflict) — 4 agent + Opus review + fix, `1adab6d`→`105fca9`. Constitution IV gaps đã fix (scheduleReminders/cancelReminders wired). T027–T034/T072/T073/T074 tick. **Residual (backlog):** overdue rescan chỉ chạy launch không chạy wake (VociApp gọi rebuildFromStorage thẳng); gate DND/screen-share chưa detect được (cần entitlement).
  - ✅ **Studio Dark redesign**: 8 màn HTML build + đẩy lên Claude Design project "Voci — Studio Dark Redesign" (b2f396b8-...). Theme tokens áp vào code (`efaea8e` — API-stable, thêm nowAccent/nowGlow/SpotlightBackground/vociMono). **MenuBarLabel restyle xong** (`6f4f116`). **PopoverView + TodayView restyle CHƯA XONG** — agent chết session limit giữa chừng, đã revert về clean HEAD, **CẦN LÀM LẠI** (đọc menubar-now.html/command-deck.html, giữ 100% binding, chỉ đổi visual). FocusOverlay/Settings/sheets restyle chưa làm.
  - ⏳ **Supabase integration ĐANG DỞ**: `supabase/config.toml` tạo xong (verify_jwt=false). **CHỜ anh Khôi authorize MCP OAuth** (URL đã gửi, scope có edge_functions:write + database:write) → rồi em apply migration `0001_parse_quota.sql` + deploy function `parse` + test. **Secrets CHỈ anh set** (Dashboard/CLI, KHÔNG vào repo): `GEMINI_API_KEY` + Apple App Store Server/App Attest keys. Project: https://nuzrpipwacravfgsiacv.supabase.co (ref nuzrpipwacravfgsiacv). Publishable key (safe-to-expose): sb_publishable_pz2_mJTispHtuvcYWTpE6g_SDHmpRUI.
  - **VIỆC CÒN LẠI:** (1) [chờ anh] authorize Supabase MCP → em deploy+migrate; (2) [chờ anh] set secrets; (3) redo restyle PopoverView+TodayView (+ FocusOverlay/Settings/sheets); (4) **Phase 5** (US3 voice-done/auto-advance/sweep — contract `phase5-contract.md` đã sẵn) → Opus review → fix; (5) **Phase 6** (orchestrator); (6) **VERIFY MAC** cụm P1–4 + restyle (`swift test` VociCore, `xcodegen`+build Voci; rủi ro: Carbon hotkey, SwiftData migration, FoundationModels, nonisolated(unsafe), 2 SwiftData container). (2026-07-16 tối)

- [ ] **★ PROGRESS FEATURE 002 (2026-07-16 — dừng vì session limit, MAI TIẾP TỤC TỪ ĐÂY):**
  Phase 1+2 + T022 đã code bằng 4 agent Sonnet song song trên nền commit spec `b0e90ed`, TẤT CẢ CHƯA build/test trên Mac:
  - ✅ **Agent A (Engine, T005–T012)**: VociCore v2 xong + self-review sạch — `Condition.swift`/`Snapshots.swift` mới, Task/NextTask/DependencyGraph rework, test suite migrate 1:1 + case v2 (afterDate boundary, external, parent-exclusion, shuffle n=500). ⚠️ **PHÁT HIỆN CẦN FIX: engine neo "deadline hôm nay" theo UTC cố định** (contract v2 lỡ bỏ param `calendar`) — user UTC+7 sẽ bị phân loại sai deadline buổi tối. ĐÃ CHỐT HƯỚNG: tái nhập `calendar: Calendar` làm tham số inject của `nextTask`/`orderedBefore` (vẫn pure), app layer truyền `Calendar.current`. Chưa sửa.
  - ✅ **Agent B (Persistence, T013–T016)**: VociTask/TaskItem v2 + `CompletionLog.swift`/`Recurrence.swift` mới + 6 validation rule trong TaskStore; migration chọn kiểu defensive in-place (giữ `dependsOn` deprecated, fold sang `.taskDone` ở fetchAll đầu, idempotent — KHÔNG dùng VersionedSchema); sửa cơ học AppState (4 chỗ: `toEngineTask()`→`snapshot()`, `dependsOn: []`→`conditions: []`) + SampleData (7 chỗ). Residual risk khai báo: read-path không re-validate conditions (trust boundary ở write-side); `anchorRecurrenceToCompletion` có param nhưng chưa UI nào gọi.
  - ✅ **Agent C (Setup/Sandbox, T001–T003)**: entitlements 5 cái (sandbox, network.client, audio-input, user-selected RW, bookmarks); URL scheme `voci://` đăng ký thẳng `Info.plist` (project dùng GENERATE_INFOPLIST_FILE: NO — không qua xcodegen info); HotkeyManager rewrite Carbon RegisterEventHotKey giữ nguyên API (AppState zero-diff, ⌃⌥M toggle); ảnh nền custom → security-scoped bookmark + cache NSImage trong @State; 3 folder placeholder Parsing/Reminders/Orchestrator. Cả file Carbon đầy `// UNVERIFIED` — verify ĐẦU TIÊN trên Mac.
  - ⚠️ **Agent D (Supabase /parse, T004+T022): CODE ĐÃ VIẾT ĐỦ FILE nhưng agent chết vì session limit TRƯỚC KHI self-review 6 mục + threat model** — `supabase/functions/parse/index.ts`, `_shared/{auth,env,gemini,http,log,quota,schema}.ts`, `migrations/0001_parse_quota.sql`, README. **MAI PHẢI: review/type-check lại toàn bộ supabase/ (coi như untrusted), đặc biệt threat model theo contract `parse-proxy.md`** (quota race atomic, JWS alg pinning, App Attest keyId, prompt injection caps, không log transcript, RLS).
  - **✅ OPUS REVIEW XONG (2026-07-16, gộp cả review supabase)**: verdict **0 ship-blocker / 3 MAJOR / 6 MINOR / 6 INFO**. Security mạnh (mọi path fail-closed, không path nào tới Gemini thiếu auth, không rò transcript/secret). Đang fix bằng 1 agent Sonnet (agentId a0800e7f). MAJOR: (1) vụ UTC calendar CHƯA được fix ở đợt đầu → đang tái nhập `calendar` param; (2) `AppState.tasks` in-memory lệch với TaskStore v2 (toggleDone/deleteTask không phản ánh recurrence-reset/parent-cascade/CompletionEvent → UI sai tới lúc relaunch) → refresh từ store.fetchAll(); (3) supabase free tier luôn 503 vì App Attest key store chưa impl → **backlog dưới** (fail-closed đúng, client T021 chưa có nên chưa ai hit /parse). MINOR đang fix: quota default 25→50 (khớp contract), lenient decode Condition (unknown kind wipe cả mảng), schema `now` bắt buộc timezone, sửa comment stale AppState:579.
  - **✅ FIXES APPLIED + COMMIT `b7a3372` (2026-07-16)**: 3 MAJOR + 4 MINOR đã sửa (calendar injection khôi phục — grep xác nhận 0 `engineCalendar` sót, 0 call site thiếu calendar; AppState refresh từ store.fetchAll(); lenient decode; quota 25→50; schema `now` bắt buộc tz; comment). T001–T016 tick `[x]`, T022 `[~]` (partial — free tier stub). Contract + data-model đã đồng bộ signature calendar.
  - **✅ PHASE 3 (US1 voice capture, T017–T026) XONG (2026-07-16, commit `dbfdfce`)**: 3 agent Sonnet song song theo contract `parsing-contract.md` (ParsedTask v2 + IntentRouter FM→Cloud→Heuristic + confirm card chips) → Opus review bắt 2 compile-blocker + 1 crash + 1 MAJOR (seam chưa khớp: HeuristicNLParser thiếu IntentParser conformance, SampleData v1, ParseCorrection thiếu trong Schema, followUpReview overflow batch) → 1 Sonnet fix apply hết + grep verify. Constitution I/II/V PASS, security sạch (text-only, decode validated, 10-task cap 3 tầng). T017–T026 tick `[x]`, T022 `[~]`. CHƯA build/test Mac.
  - **★ VIỆC CÒN LẠI (BÀN GIAO CHO ANH KHÔI):** (1) **VERIFY TRÊN MAC** — bước chặn, Windows không build được. `cd VociCore && swift build && swift test`; rồi `cd Voci && xcodegen generate && open` build target Voci. Rủi ro compile cao nhất: **Carbon hotkey** (HotkeyManager), **SwiftData migration** + Schema 3 model, **`nonisolated(unsafe)` static** trong NLParser (Swift 6 — có thể no-op nếu SDK mark Sendable), **FoundationModels `@Generable`** (macOS 26, đầy `// UNVERIFIED`), **DeviceCheck/App Attest**. Test migration data cũ dependsOn→conditions. Lỗi báo về em → Sonnet fix. (2) Sau Mac xanh: tiếp **Phase 4 (US2 reminders, T027–T034)** rồi Phase 5–8 (mỗi phase tuần tự vì đều wire vào AppState/Views chung; song song trong từng phase). (2026-07-16)
- [ ] **002 Phase-3 deferred (từ Opus review, không chặn):** (a) [Mi-2] `IntentRouter.lastCloudQuotaNote` (FR-012 note 429) chưa được AppState đọc/hiển thị — vô hại hôm nay vì cloud inert; wire khi có credential provider. (b) **Cloud parse hiện INERT** (chỉ FM→Heuristic) vì `ParseCredentialProvider` (StoreKit JWS + DeviceCheck composite) chưa build — thuộc Phase 8/T051; gate đã wire sẵn. (c) [I-1] App Attest `clientDataHash` dùng nonce ngẫu nhiên chưa bind vào request body — reconcile khi có endpoint `/attest/register`. (d) FM `GeneratedParsedTask` scoped-down: bỏ `reminderOverride`, chỉ 1 condition/task (vs Cloud array) — first pass, nâng sau. (2026-07-16)
- [ ] **002 backlog từ Opus review (deferred, không fix đợt này):** (a) **[MAJOR→backend follow-up] Supabase free-tier cloud parse chưa hoạt động** — `UnimplementedAppAttestKeyStore` luôn trả null → mọi token free ra 503 (fail-closed đúng). Cần: endpoint `/attest/register` + bảng Postgres lưu public key theo keyId (App Attest), rồi `/parse` verify assertion + dùng SHA-256(keyId) làm quota key. Chưa gấp vì client `CloudParser` (T021) chưa tồn tại nên chưa ai gọi /parse. (b) [MINOR] quota consume TRƯỚC khi gọi Gemini → 502 upstream vẫn tốn 1 lượt; cân nhắc decrement-on-failure hoặc increment-after-success khi free-tier key store xong. (c) [MINOR] HotkeyManager: `deinit` teardown không reachable khi còn `passRetained` (benign vì owner là singleton app-lifetime; nếu sau này owner khác giữ HotkeyManager thì leak Carbon registration — phải gọi `stop()` tường minh). (d) [INFO] Deno `npm:@apple/app-store-server-library` + `appattest-checker-node` + `toNodeBuffer` no-op: verify chạy được trên Supabase Edge runtime lúc deploy. (e) [INFO] `gemini.ts` model id `gemini-3.1-flash-lite` verify tồn tại trong AI Studio trước deploy (env-overridable). (f) [INFO] `parse_rate_limit` table chưa có retention → cần pg_cron prune trước khi có traffic thật. (g) [INFO] read-path TaskStore không re-validate conditions/parentId — accepted trust boundary (write-side validate + DFS visited-set bounded), không cần hardening. (2026-07-16)

- [ ] **Điều chỉnh vision/pricing theo deep-research thị trường 2026-07-16** (xem memory `market-2026-competitive-threats.md`). Các quyết định cần anh Khôi chốt trước khi sửa `docs/product-vision-v2.md`:
  - **Đổi headline định vị** từ "voice-first workflow command center" (nhấn cả 4 trụ ngang nhau) → nhấn **ADHD/executive-function cho người lớn năng suất cao**; hạ AI-agent orchestration xuống *feature*, không phải trụ ngang hàng (đã bị Sherlock: Claude Code voice dictation + Agent View đều free first-party; Conductor free macOS; Spokenly voice-MCP).
  - **Xem lại cấu trúc freemium** "mọi feature free, chỉ bán cloud compute": data cho RPI $0,38 (kém hard-paywall 8×) + conversion freemium median 2,1%. Cân nhắc paywall onboarding mềm / gate vài feature scale-polish hẳn. TỐI THIỂU: hiện đếm quota (đã chốt) — nó là quảng cáo Pro.
  - **Sửa giả định mô hình doanh thu**: đừng plan quanh 5% conversion (= 2,4× benchmark freemium); plan cận thực 2–3%. Mục tiêu thực tế hơn **$300–800 MRR/12 tháng** (không phải $500–2K; đó là top-decile — chỉ 17,3% app mới chạm $1K MRR/2 năm). Comparable: Llama Life 4 năm mới ~$4,25K MRR.
  - **Cân nhắc nâng giá** $4,99→$6,99–7,99/mo (productivity median $9,99; voice $8–15; higher price convert trial tốt hơn). $39,99/yr giữ nguyên OK.
  - **Trial**: cân nhắc 14→17+ ngày (dải 17–32 ngày convert 42,5% vs 25,5% trial ngắn).
  - Theo dõi rủi ro chí mạng: Anthropic/OpenAI mở Agent View+voice sang non-dev; Blabby AI/Tiimo thêm voice+engine; Apple Sherlock voice-to-task ở macOS 27. (2026-07-16)

- [ ] **★ RENAME APP "Voci" — tên trùng nặng trên App Store (2026-07-16, đang chờ anh Khôi chốt tên):**
  Phát hiện "Voci" trùng ≥4-5 app iOS cùng tên, trong đó có [Voci: AI Voice Note Taker](https://apps.apple.com/us/app/voci-ai-voice-note-taker/id6756264008) (đúng ngách) + cả cụm Vocy/Vocci/Voca/Voicy. Discoverability chết + rủi ro Apple reject/trademark. **Quyết: rename dứt khoát khi còn pre-launch (chưa có brand equity).**
  - **Hướng đặt tên đã chốt (2026-07-16):** BỎ tên mô-tả-chức-năng (voice/task/nudge/momentum đều đã cháy — sweep 2 vòng/27 tên xác nhận cả từ điển lẫn coined phổ biến đều bị chiếm, thường bởi app pitch y hệt: Ember "Now & Next", Propelo ADHD, Nudge ADHD Task Starter...). Đi **coined/ẩn-dụ miền-bất-ngờ** (kiểu Notion/Linear/Bear).
  - **Bộ lọc đúng (học được):** tên trơn phải CHƯA có app nào trên App Store (Apple bắt tên duy nhất toàn store) + không brand/startup nổi trùng tên + domain khả thi. "Lane có trống không" là chưa đủ.
  - **Kết quả sweep (subagent, App Store + domain + trademark cơ bản):**
    - 🟢 **Onso** — GREEN duy nhất: không app tên trơn, không brand nổi, không đụng category, domain khả thi. Nhược: coined vô nghĩa (blank canvas), cần marketing gắn nghĩa.
    - 🟡 **Volar** ("bay/cất cánh", momentum) — tên trơn còn trống nhưng dính bóng hãng bay Volaris + Volar.js (dev tool, khác audience).
    - 🟡 **Elan / Impetus / Onset** — tên trơn trống nhưng có brand nổi trùng tên giữ .com (Elan ski/chip/credit; Impetus Technologies; Onset Ventures).
    - 🔴 Loại (đã có app tên trơn y hệt hoặc brand thống trị): Voci, Wren (voice-chat app + wren.co), Corva (app dầu khí), Cusp/Verto/Fero/Sona/Kairos, Cairn/Ember/Vane/Helm/Lark/Skiff, Koru (Air NZ), Momenta/Nira/Vireo...
  - **✅ CHỐT TÊN: "Volar" (2026-07-16).** Volar = "bay/cất cánh" (Tây Ban Nha) → hợp momentum. Tên trơn "Volar" trên App Store còn trống (hiện chỉ có VolarApp/Volar Dating/Volar sin Miedo — đều có hậu tố). Bóng mờ chấp nhận: hãng bay Volaris (khác class) + Volar.js (dev tool, khác audience) + nghĩa y khoa "volar" (lòng bàn tay — vô hại). CHƯA verify sâu domain/USPTO (anh Khôi skip bước subagent verify — làm sau trước khi nộp store).
  - **FOOTPRINT ĐỔI TÊN (grep 2026-07-16): 1094 chỗ / 94 file.** Phân tầng:
    - **Tier 1 — danh tính app (ship-blocking):** `Voci/project.yml` (name, `bundleIdPrefix: app.voci`, `PRODUCT_BUNDLE_IDENTIFIER: app.voci.Voci`, `PRODUCT_NAME: Voci`, target/scheme `Voci`+`VociTests`, package key `VociCore`); `Voci/Resources/Info.plist` (CFBundleIdentifier/Name/DisplayName/Executable `Voci`, CFBundleURLName `app.voci.Voci`, URL scheme `voci`); usage-description strings nhắc "Voci" (mic/speech). **Quyết định cần: bundle id mới** (`app.volar.Volar`? — reverse-DNS phải theo domain anh Khôi sở hữu; chốt trước lần upload đầu).
    - **Tier 2 — Swift module + type (ripple code toàn bộ):** module `VociCore`→`VolarCore` (`VociCore/Package.swift` name+product+target+testTarget, dir `Sources/VociCore/` + `Tests/VociCoreTests/`, MỌI `import VociCore` + `VociCore.Task`/`VociCore.nextTask`); type: `VociApp`(@main), **`VociTask`(SwiftData @Model — đổi tên class = đổi entity name → cần migration; pre-launch nên xoá store dev là xong, phải nhớ)**, `VociAccent`(Theme), `VocIcon`; dir app `Voci/`→`Volar/`.
    - **Tier 3 — non-ship (đổi thong thả):** docs/*.md, specs/**, design/*.jsx, `.idea/voci.iml`, README, CLAUDE.md, memory.
    - **Tier 4 — thủ công/ngoài repo (anh Khôi trên Mac):** reserve tên "Volar" trong App Store Connect (Apple là bên chốt cuối); `xcodegen generate` lại sau khi sửa project.yml; cân nhắc đổi tên repo dir.
  - **BẪY khi execute:** (a) KHÔNG blind sed `Voci→Volar` case-insensitive — `VocIcon` chứa chuỗi "vocI" sẽ bị hỏng thành "Volarcon". Phải dùng mapping có phân biệt hoa/thường + word-aware (giao Sonnet làm cẩn thận theo bảng mapping, không sed mù). (b) `voci://`→`volar://` phải sửa cả `specs/.../contracts/app-links.md` + handler (hiện chưa có onOpenURL). (c) SwiftData entity rename (xem trên).
  - **✅ ĐÃ THỰC HIỆN RENAME CODE (2026-07-16, anh Khôi chốt "làm luôn"):** 1 Sonnet agent chạy full scope, mapping case-sensitive (`Voci→Volar`/`voci→volar`/`VOCI→VOLAR` + tay `VocIcon→VolarIcon`), trừ `backlog.md`. Kết quả: 104 mục trong `git status` (10 rename thuần R, 65 RM, 29 M), 94 file đổi nội dung; đổi tên đủ 12 dir/file (dir `Voci/`→`Volar/`, `VociCore/`→`VolarCore/` + inner Sources/Tests, VociApp/VociTask/VocIcon/entitlements + docs/design/specs). Sonnet bắt thêm token ẩn `init(voci hex:)`/`Color(voci:)`→`volar`. **Opus verify độc lập: `grep -i voci` chỉ còn trong `backlog.md`; 0 file tên voci; 0 `Voci`/`VocIcon` ngoài backlog.** project.yml/Info.plist/Package.swift đúng. **CHƯA COMMIT** (để anh review working tree). CHƯA build Mac.
    - **BÀN GIAO anh Khôi (thủ công, không làm trên Windows được):** (1) trên Mac trước lần chạy đầu: **xoá store SwiftData dev cũ** (VolarTask đổi entity name → schema mismatch); (2) `cd Volar && xcodegen generate` lại (đổi cả cấu trúc target/scheme); (3) build verify; (4) **reserve tên "Volar" trong App Store Connect** (Apple chốt cuối); (5) chốt **domain thật** cho Volar rồi đổi `PRODUCT_BUNDLE_IDENTIFIER`/`bundleIdPrefix` từ placeholder `app.volar` sang reverse-DNS đúng nếu cần; (6) verify sâu domain/USPTO trước khi cam kết chi tiền; (7) review + commit. Minor còn lại: filename spec/doc đã đổi theo (link nội bộ tự nhất quán vì content cũng replace). (2026-07-16)
- [ ] Cân nhắc thêm `.claude/` (đặc biệt `.claude/settings.local.json`) vào `.gitignore` để tránh commit nhầm thông tin nhạy cảm. (2026-07-12 — cảnh báo từ `specify init`)
- [ ] Cân nhắc migrate `docs/voci-voice-task-engine-spec.md` sang flow Spec Kit (constitution/specify) nếu muốn dùng spec-driven đầy đủ. (2026-07-12 — spec doc viết trước khi init Spec Kit)
- [ ] FR-013 (feature 001 nextTask) phần **app/persistence-side**: khi xóa task phải strip `id` của nó khỏi `dependsOn` của mọi task khác trong storage, và **notify user một lần** khi việc này đổi trạng thái blocked của task phụ thuộc. `VociCore` (pure engine) chỉ xử lý phần eligibility (coi archived/deleted là resolved); phần mutate storage + notify thuộc feature persistence/reminder, chưa có task. (2026-07-12 — hoãn vì ngoài scope engine, ghi lại theo phân tích /speckit-analyze N1)
- [ ] T018 (feature 001): viết `VociCore/README.md` mô tả public API, link tới `specs/001-nexttask-engine/contracts/nexttask-api.md`. Agent implement bỏ qua vì nằm ngoài danh sách file cốt lõi. (2026-07-12)
- [ ] Verify `VociCore` trên Mac: `cd VociCore && swift build && swift test` — code viết trên Windows, CHƯA compile/test lần nào. Check đầu tiên nếu lỗi: `Task` trùng tên `_Concurrency.Task`. (2026-07-12)
- [ ] Verify `Voci/` app (Phase 1 foundation) trên Mac: `cd Voci && xcodegen generate && open Voci.xcodeproj`, build target **Voci**. Toàn bộ code viết trên Windows, CHƯA compile lần nào. Rủi ro cao nhất: `VociCore.Task` vs `_Concurrency.Task` (đã fully-qualify `VociCore.Task`/`VociCore.nextTask` trong `TaskItem.swift`/`AppState.swift`/`TaskStore.swift` để tránh đụng độ, nhưng chưa test); `@Environment(AppState.self) var x: AppState?` optional-environment syntax trong `Components.swift` (Observation framework — cú pháp optional form có thể cần Xcode 26 mới hỗ trợ đúng, cần check); `#Predicate<VociTask> { $0.id == id }` trong `TaskStore.swift`. (2026-07-12)
- [ ] `Voci/project.yml` chưa có `Assets.xcassets` (AppIcon/AccentColor) — bị bỏ qua vì không nằm trong danh sách 14 deliverable bắt buộc của Phase 1. Cần thêm khi có icon thật, kèm cập nhật `sources:` trong `project.yml`. (2026-07-12)
- [ ] `AppState.confirmSave()` hiện set `deadline: nil` cho task mới tạo từ voice, vì `ParsedTask.when` là `String` hiển thị ("Tomorrow · 2:00 PM"), không phải `Date` — engine `nextTask` sẽ không thấy near-term deadline cho task vừa tạo bằng giọng nói cho tới khi có parser thông minh hơn resolve ra `Date` thật. Tạm chấp nhận cho Phase 1; theo dõi khi nâng cấp `NLParser`. (2026-07-12)
- [x] Focus session countdown (`focusSecondsLeft` đếm ngược mỗi giây) — đã implement trong `Sources/Views/FocusOverlay.swift` (Phase 2, Agent C) bằng `Timer.publish(every: 1...).autoconnect()`. (2026-07-12)
- [ ] `HeuristicNLParser` (`Sources/Model/NLParser.swift`) là parser heuristic đơn giản (NSDataDetector + keyword). Theo đúng spec, đây là "smarter parser is a later swap behind the protocol" — cân nhắc parser tốt hơn (on-device ML) sau khi có usage thật. (2026-07-12)
- [ ] `Sources/Speech/HotkeyManager.swift` (Phase 2, Agent C) đăng ký global hotkey ⌃⌥Space qua `NSEvent.addGlobalMonitorForEvents` — chỉ hoạt động system-wide (khi app khác đang frontmost) nếu user đã cấp quyền **Accessibility** cho Voci trong System Settings → Privacy & Security → Accessibility. Chưa có code request/check quyền này (không có API để chủ động detect trước) và chưa wire vào `AppDelegate`/`VociApp.swift` (để dành Phase 3). Đồng thời `HotkeyManager` dùng `Task { @MainActor in }` bên trong closure của `NSEvent` monitor để thoả Swift 6 strict concurrency — CHƯA build/test trên Mac, đây là điểm rủi ro cao nhất trong file này nếu compiler không chấp nhận cách hop-actor đó. (2026-07-12)
- [ ] `Sources/Audio/AmbientSound.swift` và `Sources/Speech/VoicePlayback.swift`/`HotkeyManager.swift` (Phase 2, Agent C) đều CHƯA compile/test trên Mac — rủi ro cụ thể: `AVAudioUnitEQ`/`AVAudioMixerNode` graph connect trong `AmbientSound`, `AVSpeechUtteranceDefaultSpeechRate` scaling trong `VoicePlayback`, và `.onKeyPress`/`.focusable()` trong `FocusOverlay.swift` (cần Xcode 26 / macOS 26 SDK để chắc API tồn tại đúng như giả định). (2026-07-12)

- [ ] **Phase 3 (Opus, integration) hoàn tất — toàn bộ app Voci macOS (Phase 1–3) đã implement xong nhưng CHƯA build/test lần nào trên Mac.** Đã sửa `Theme.swift` (thêm `Equatable, Hashable` cho `VociAccent`/`Density`/`GlassLevel`), `AppState.swift` (wire `VoicePlayback`/`AmbientSound`/`HotkeyManager`/`SpeechCapture` thật, voice feedback, `toggleAmbientSound()`, `setFrog()`, `activateServices()`), `VociApp.swift` (mount `MenuBarLabel`/`TodayView`/`SettingsView` thật + menu content + `UNUserNotificationCenter` auth), và tạo `Voci/README.md`. Rủi ro cần check đầu tiên khi build trên Mac:
  - `VociCore.Task` vs `_Concurrency.Task` trùng tên (đã fully-qualify ở `TaskItem.swift`/`AppState.swift`/`TaskStore.swift`, nhưng chưa compile-test).
  - `#Predicate` trong `TaskStore.swift` (SwiftData query macro — cú pháp có thể cần điều chỉnh theo Xcode 26 SDK thật).
  - Swift 6 `@Sendable`/`nonisolated(unsafe)` trong `SpeechCapture.swift` (audio tap + recognition-task callback), `HotkeyManager.swift` (`NSEvent` global/local monitor closures), `AmbientSound.swift` (fade-ramp `Task`) — tất cả đều dùng cách hop-actor thủ công, chưa được compiler thật kiểm chứng.
  - `.onKeyPress`/`.focusable()` trong `FocusOverlay.swift` — cần xác nhận API tồn tại đúng như giả định trên macOS 26 SDK.
  - `MenuBarExtra`/`@Environment(\.openWindow)` behavior trong `VociApp.swift` (menu content mới thêm ở Phase 3, dùng `openWindow(id: "main")` từ trong `MenuBarExtra`'s content closure — chưa test có mở đúng `Window("Voci", id: "main")` hay không).
  - `AppState.activateServices()` gọi `hotkey.start(appState:onKeyUp:)` mà **không** truyền `onKeyDown` (vì `HotkeyManager.start` đã tự gọi `appState.startCapture()` trực tiếp trên key-down rồi — truyền thêm `onKeyDown` gọi `startCapture()` lần nữa sẽ double-call). Cần xác nhận hành vi này đúng ý khi test thật trên Mac.

  Follow-up chưa làm (ngoài scope Phase 3 "glue only"):
  - `NLParser` thông minh hơn, resolve `ParsedTask.when` (String hiển thị) thành `Date` thật cho `deadline` — hiện `confirmSave()` vẫn set `deadline: nil` cho task tạo bằng giọng nói (xem mục cũ bên trên).
  - Ambient sound polish thật (âm thanh hiện tại chỉ là white/brown noise tổng hợp đơn giản, chưa tinh chỉnh theo từng mode rain/snow/embers).
  - [x] `MorningFrogView`, `TaskBreakdownView`, `NotificationView` — **đã mount vào `VociApp.swift`/`TodayView.swift`** (2026-07-12, follow-up, CHƯA build/test trên Mac):
    - `MorningFrogView` = daily first-launch sheet: `.sheet(isPresented: appState.showMorningFrog)` trên `Window("Voci", id: "main")`, gated once/day qua `@AppStorage("morningFrogLastShown")` (so với ngày hôm nay, chỉ show khi đã onboard xong và `openTasks` không rỗng). Pick candidate → `AppState.pickFrog(_:)` (gọi `setFrog` + đóng sheet); Skip/voice-answer → `AppState.dismissMorningFrog()`.
    - `TaskBreakdownView` = context-menu → sheet: `TaskRow` có `.contextMenu` mới ("Break down into steps…" → `appState.showBreakdown = true`, cộng "Mark done"/"Delete" tiện thể). "Save all as tasks" → `AppState.saveBreakdown(_:)` (persist sample step titles thành `TaskItem` thật, `.medium`/`.later`, không deadline/duration); Cancel/Edit → đóng sheet. Lưu ý: sheet hiện KHÔNG nhận biết task nào được click — breakdown luôn hiển thị sample "Launch landing page" cố định, chưa wire theo task thật (theo đúng phạm vi yêu cầu của lần mount này).
    - `NotificationView` = in-app banner: parameterized (`title`/`timing`/`onDone`/`onSnooze`/`onReschedule`), hiển thị qua `TodayView`'s `.overlay(alignment: .topTrailing)` khi `appState.reminderBanner != nil`, tự dismiss sau 5s (`Task.sleep`) hoặc khi bấm nút. Trigger thủ công qua menu-bar "Preview reminder" → `AppState.showReminderPreview()`. **Lịch notification thật qua `UNUserNotificationCenter` (deliver theo task deadline) vẫn CHƯA làm** — vẫn chờ `NLParser` resolve `deadline: Date` thật từ giọng nói trước (xem mục cũ ở trên); hiện `AppDelegate` chỉ xin quyền, chưa từng schedule request nào.
  - `TodayView.swift`'s ambient-sound toolbar button vẫn dùng `@State private var ambientSoundPlaying` cục bộ (comment ghi rõ là "UI-only placeholder") thay vì gọi `appState.toggleAmbientSound()`/đọc `appState.ambientSound.isPlaying` thật — `toggleAmbientSound()` đã có ở `AppState` từ Phase 3 nhưng chưa được `TodayView` dùng (nằm ngoài phạm vi sửa đổi cho phép của Phase 3 — chỉ được sửa `Theme.swift`/`AppState.swift`/`VociApp.swift`). (2026-07-12)

- [x] Custom app background giờ đã user-controllable: **Settings → Appearance → Background** (segmented None/Rain/Snow/Fireflies/Custom, bind `appState.ambient` qua `setAmbient(_:)`) + row "Custom image" (chỉ hiện khi mode = Custom) với `NSOpenPanel` file picker (`chooseImage`) và nút Remove. Persist qua `UserDefaults` (`voci.ambient`/`voci.customImageURL`), đọc lại trong `AppState.init`. Rendering (`AmbientBackground`/`CustomImageLayer`/glass panels trong `TodayView`) đã có sẵn từ trước, chỉ thiếu control UI — đã thêm. Đổi `AmbientMode` sang `String`-backed + `CaseIterable`/`Identifiable` để bind trực tiếp vào `Segmented<AmbientMode>`. App không sandbox nên dùng raw file path (`url.path`) — nếu sau này bật App Sandbox thì `NSOpenPanel` result cần security-scoped bookmark thay vì path thô (đã ghi chú trong code). CHƯA build/test trên Mac — mã viết trên Windows theo quy tắc split-env. (2026-07-12)
- [x] Hạ `deploymentTarget` macOS 26.0 → 14.0 trong `Voci/project.yml` (3 chỗ: options.deploymentTarget, target Voci, MACOSX_DEPLOYMENT_TARGET). Lý do: code KHÔNG dùng API riêng của macOS 26 — "Liquid Glass" hiện được fake bằng `.thinMaterial`/`.thickMaterial`/`.ultraThinMaterial` chuẩn (macOS 12+); trần API thực tế là macOS 14 (`@Observable`/`@Environment(AppState.self)`/`@Bindable`, `.onKeyPress`). Hạ target để chạy được máy Mac ~2018 trở lên (Sonoma) thay vì kén macOS 26 (chỉ máy 2019+). Giao diện không đổi. CHƯA build/test trên Mac. (2026-07-13)
- [ ] Nâng lên **Liquid Glass THẬT** (API macOS 26: `.glassEffect()`, `GlassEffectContainer`, `.buttonStyle(.glass)`, `.interactive()`) — enhancement/polish, để dành làm sau, sẽ phải nâng lại `deploymentTarget` về 26.0 khi làm. Hai chỗ đáng nhất trên Voci: (1) **Popover menu-bar** (`Sources/Views/PopoverView.swift`) vì nó nổi đè lên app/wallpaper khác → khúc xạ + specular highlight thật; (2) **nút mic/waveform khi giữ ⌃⌥Space để nói** — bọc `.glassEffect().interactive()` cho hiệu ứng phồng/sáng phản hồi voice-first (hiện chưa bọc glass). Các panel trong cửa sổ nền tối (MorningFrog/TaskBreakdown/Notification/Sidebar/FocusOverlay) nâng cũng gần như không thấy khác nên ưu tiên thấp. (2026-07-13)
- [ ] **Verify trên Mac:** `AVAudioApplication.requestRecordPermission()` (async, macOS 14+) có thực sự bật TCC prompt mic trên macOS không (API gốc là của iOS/AVAudioSession-world; docs ghi macOS 14.0+ và code review 2026-07-13 đã chuyển sang biến thể async). Nếu không prompt → thay bằng `AVCaptureDevice.requestAccess(for: .audio)` trong `SpeechCapture.requestAuthorization()`. (2026-07-13 — không thể verify trên Windows)
- [ ] Perf: `NSImage(contentsOf:)` decode ảnh custom background ĐỒNG BỘ trên main thread **mỗi lần body render** — cả `CustomImageLayer` (`AmbientBackground.swift`) lẫn thumbnail trong `SettingsView.swift`. Với wallpaper lớn sẽ giật UI mỗi khi AppState đổi. Nên cache `NSImage` theo URL (hoặc load async 1 lần). Không sửa ngay vì cần chọn chỗ đặt cache (view `@State` vs AppState). (2026-07-13)
- [ ] Popover quick-capture hiển thị hint "Esc cancel" nhưng phím Esc chỉ thật sự hoạt động ở state `.parsed`/`.saving`/`.error` (các nút có `.keyboardShortcut(.cancelAction)`); ở `.recording`/`.parsing`/`.done` không có handler Esc — chỉ click scrim mới cancel được. Cần quyết cách bắt Esc toàn popover (hidden button vs `.onKeyPress(.escape)` + focus). (2026-07-13)
- [ ] Thứ tự task không nhất quán sau relaunch: trong session `addTask` chèn đầu danh sách (mới nhất trước), nhưng `TaskStore.fetchAll()` sort `createdAt` **ascending** → sau khi mở lại app, task mới nằm cuối. Cần chốt thứ tự chuẩn (mới nhất trước?) rồi đổi 1 trong 2 phía. (2026-07-13)
- [ ] Morning frog bị skip đúng ngày user vừa onboard xong: `.task` của main window chạy trước khi `hasOnboarded` flip nên hôm đó không hỏi frog (hôm sau mới hỏi). Nếu muốn hỏi ngay sau onboarding, cần trigger lại check sau `onComplete`. (2026-07-13)
- [x] Nút "Hold to speak" (Sidebar) và "Hold Space to answer" (MorningFrog) là **click** → `startCapture()` nhưng không có tương tác "nhả" tương ứng. ĐÃ LÀM: chuyển toàn bộ sang **toggle** (tap bật thu / tap dừng+parse) + hotkey ⌃⌥M cũng toggle; label động "Tap to speak"/"Tap to stop". Commit `ee75841` (2026-07-14).
- [ ] Hiệu ứng "thở" (pulse glow) cho icon mic trên menu bar đã bị gỡ khỏi `MenuBarLabel.swift` vì animation `repeatForever` đặt trực tiếp trên label của `MenuBarExtra` gây vòng lặp layout AppKit ("-layoutSubtreeIfNeeded ... already being laid out") và treo main thread khi mở app. Nếu muốn phục hồi pulse sau này, phải làm theo cách AppKit-safe (ví dụ animate image của `NSStatusItem.button` thay vì `withAnimation(...).repeatForever()` trong SwiftUI menu-bar label). (2026-07-13)
- [ ] `AppState.openDictationSettings()` (`Sources/App/AppState.swift`) thử 2 URL pane System Settings (`com.apple.Keyboard-Settings.extension`, `com.apple.preference.keyboard`) — URL pane Dictation khác nhau theo phiên bản macOS, CHƯA verify trên máy thật. Nếu cả 2 candidate đều fail (không mở đúng pane Dictation) trên macOS của anh Khôi, cần tìm URL đúng (hoặc fallback mở System Settings chung rồi hướng dẫn user tự vào Keyboard → Dictation). Đồng thời: nhận giọng giờ ưu tiên on-device trước, chỉ fallback sang server Apple khi user bấm "Use Apple servers" (đã nới nguyên tắc on-device-only ban đầu — audio rời máy lên Apple khi user đồng ý, cờ `allowServerFallback`/`allowServerRecognition` được persist qua `UserDefaults`). (2026-07-14)
- [ ] **App Store — Privacy Nutrition Label:** khi nộp app phải khai báo đúng quyền riêng tư. Vì chế độ fallback server (`allowServerRecognition = true`) **gửi audio lên server Apple**, phải khai "Audio Data" được thu thập/gửi đi trong App Privacy (App Store Connect). Nếu để on-device-only thì không cần khai. Khai sai → bị reject. `SFSpeechRecognizer` là public API, miễn phí, không cần entitlement/backend riêng — chỉ cần usage description (đã có `NSSpeechRecognitionUsageDescription` + `NSMicrophoneUsageDescription`) và privacy label đúng. Giới hạn server: ~1 phút audio/request, ~1000 request/giờ/thiết bị (dư cho use case tạo task ngắn). (2026-07-14)
- [ ] **Nâng lên `SpeechAnalyzer`/`SpeechTranscriber` (macOS 26):** API nhận giọng on-device thế hệ mới của macOS 26, KHÔNG lệ thuộc toggle Dictation kiểu cũ như `SFSpeechRecognizer` (tránh hẳn lỗi "Siri and Dictation are disabled") và không giới hạn 1 phút/request. Sẽ xoá được nhu cầu fallback server + flow consent hiện tại cho máy macOS 26+. Chưa làm vì đang target macOS 14 (`SFSpeechRecognizer` là mẫu số chung 14+); cân nhắc dùng có điều kiện `if #available(macOS 26, *)` khi muốn nâng, hoặc khi nâng `deploymentTarget`. (2026-07-14)
- [ ] **Cân nhắc model nhận giọng chuyên tiếng Việt (PhoWhisper/whisper.cpp/WhisperKit)** thay cho `SFSpeechRecognizer` của Apple, để tăng chính xác cho tiếng Việt & accent khó. Đã thêm chọn ngôn ngữ (Settings ▸ General ▸ "Recognition language", gồm vi-VN) + vài tinh chỉnh `SFSpeechAudioBufferRecognitionRequest` (`taskHint = .dictation`, `addsPunctuation` macOS 13+) cho `SpeechCapture`, nhưng vẫn là engine của Apple. **PhoWhisper** (VinAI) hoặc Whisper qua **WhisperKit**/**whisper.cpp** là featureset lớn hơn nhiều: model weights ~vài trăm MB phải bundle/download, thêm dependency (CoreML/Metal runtime), và cần một lớp abstraction mới phía sau `NLParser`/speech layer để có thể chọn engine (Apple vs Whisper) mà không phá flow consent on-device/server hiện có. Để sau; hiện vẫn dùng `SFSpeechRecognizer` (server cho tiếng Việt qua flow consent `allowServerFallback`/`allowServerRecognition`). (2026-07-14)
  - **CHỐT HƯỚNG (2026-07-14):** đã xác nhận SFSpeechRecognizer (kể cả server) sai nhiều với accent tệ → sẽ chuyển sang **WhisperKit** (argmaxinc, **MIT**, Whisper on-device qua CoreML/Neural Engine, Swift package thuần). Lợi ích kép: chính xác hơn hẳn accent + tiếng Việt, **on-device** nên bỏ luôn nhu cầu server/consent + Dictation.
  - **AUTO-DETECT NGÔN NGỮ (yêu cầu 2026-07-14):** muốn "nói tiếng gì cũng tự hiểu (Anh/Việt) + chính xác" — `SFSpeechRecognizer` KHÔNG làm được (bắt buộc chỉ định locale trước; sai locale → transcript rác; kể cả `SpeechAnalyzer` macOS 26 cũng phải chỉ định locale). Đây là **feature có sẵn của Whisper** (tự detect ngôn ngữ từ audio rồi transcribe, đa ngôn ngữ 1 model) → là **động lực chính thứ 2 (cùng accent)** để làm WhisperKit. Giải pháp tạm trước khi có Whisper: chọn ngôn ngữ thủ công (Settings) hoặc thêm nút chuyển nhanh EN⇄VI trên popover/sidebar (vẫn thủ công).
  - **Model ưu tiên: small/base (~150MB)** (không phải large-v3-turbo ~600MB) để giữ nhẹ.
  - **CODE-SWITCHING Anh-Việt (quyết định 2026-07-14):** chấp nhận **"đủ tốt"**, KHÔNG phải must-have → **giữ model small/base nhẹ**, KHÔNG lên medium/large. Câu trộn nặng 50-50 có thể sai (không engine tiêu dùng nào — kể cả Whisper — đảm bảo chính xác cho code-switching); câu chủ đạo 1 ngôn ngữ + chèn vài từ ngoại lai thì Whisper multilingual xử lý ổn. Whisper small vẫn hơn hẳn SFSpeechRecognizer (không khoá 1 locale). UX: khuyến khích mỗi câu 1 ngôn ngữ chủ đạo.
  - **QUAN TRỌNG — tối ưu app size cho App Store:** KHÔNG bundle model vào app (sẽ phồng app 150MB+). Cách giữ app nhỏ khi lên store: (a) **tải model on-first-run** từ Hugging Face/CDN rồi cache vào Application Support; hoặc (b) **On-Demand Resources (ODR)** của Apple; hoặc (c) host model tự quản. Cân nhắc **quantize** model để nhỏ hơn nữa. Cần UI trạng thái tải model + xử lý offline lần đầu.
  - **Ràng buộc phần cứng:** WhisperKit cần **Apple Silicon** (Neural Engine); máy Intel (mình đã hạ target macOS 14 hỗ trợ một số máy Intel) phải **fallback về `SFSpeechRecognizer`** → cần abstraction chọn engine theo khả năng máy.
  - **PhoWhisper cho tiếng Việt** (WER large ~8%, fine-tune 844h đa giọng): tốt nhất cho VN nhưng ở dạng PyTorch/HF → phải **convert sang CoreML** cho WhisperKit + switch model theo ngôn ngữ. Lưu ý: benchmark 2026 cho thấy **Whisper large-v3 thắng PhoWhisper cho code-switch Việt-Anh** → nếu đã có tầng Groq large-v3 (dưới) thì PhoWhisper bớt cần thiết.
  - **★ KIẾN TRÚC CHỐT (2026-07-14) — speech engine 2 tầng freemium:**
    - **Free = on-device**: nhúng/tải **Whisper small/base** (WhisperKit) chạy trên máy — miễn phí, riêng tư, offline; auto-detect ngôn ngữ OK; code-switch best-effort (câu trộn nặng chấp nhận sai).
    - **Paid = cloud Groq**: dùng **Groq Speech-to-Text** cho đa ngôn ngữ + detect chuẩn + code-switch tốt. Model `whisper-large-v3-turbo` **~$0.04/giờ audio** (rẻ hơn OpenAI ~9x), 99+ ngôn ngữ, auto-detect (bỏ param `language` để tự nhận), 216x realtime, free tier 2000 req/ngày; endpoint **OpenAI-compatible `/audio/transcriptions`**. Dùng `whisper-large-v3` (thay vì turbo) khi cần code-switch Việt-Anh tốt nhất. Cần: quản lý **API key** (không hardcode — server proxy hoặc user key), upload audio, **gate theo tier trả phí** (StoreKit/entitlement), disclose audio-lên-mạng trong **App Store privacy label**.
    - **Abstraction cần làm**: protocol `SpeechEngine` với 3 impl — `SFSpeechRecognizer` (fallback, máy Intel / khi chưa tải model), `WhisperKitEngine` (free on-device), `GroqEngine` (paid cloud). Route theo tier user + khả năng máy, đặt SAU lớp speech hiện tại, không phá `NLParser`/flow capture.
    - **ĐÃ LÀM (2026-07-14) — tầng Groq (paid) layer, CHƯA build/test trên Mac:** thêm 3 file `Sources/Speech/`:
      - `SpeechEngine.swift` — `protocol SpeechEngine` (@MainActor, AnyObject): start/stop + `onFinal`/`onError`/`requestAuthorization` + `supportsPartialResults`. Surface khớp `SpeechCapture` sẵn để sau này `SpeechCapture` conform 1 dòng. `onPartial` là best-effort (batch engine không bắn partial).
      - `GroqTranscriptionClient.swift` — network layer thuần (OpenAI-compatible multipart `/audio/transcriptions`), `GroqModel` (`largeV3`/`largeV3Turbo`), `GroqTranscriptionError`, và `GroqCredentialProvider` (inject endpoint+token → **không hardcode key**, hướng proxy). Có `EnvironmentGroqCredentialProvider` (dev: đọc `GROQ_BASE_URL`/`GROQ_API_KEY`/`GROQ_PROXY_TOKEN` từ env/UserDefaults, fallback trỏ thẳng Groq). **KHÔNG gửi `language`** → auto-detect vi↔en.
      - `GroqEngine.swift` — `SpeechEngine` batch impl: `AVAudioRecorder` → temp .m4a (16kHz mono AAC) → upload; primary `large-v3`, fallback `turbo` CHỈ khi lỗi 5xx/transport; session-token guard chống race stop/restart; xoá temp file sau khi đọc.
      - Follow-up CHƯA làm (deferred): (1) cho `SpeechCapture` conform `SpeechEngine` (+`supportsPartialResults=true`); (2) route theo tier trong `AppState`/`PopoverView` (batch engine cần UI "listening…" thay live caption); (3) **StoreKit** gate tier trả phí; (4) dựng **proxy backend thật** + provider đọc token từ Keychain (thay `EnvironmentGroqCredentialProvider` dev); (5) UI Settings chọn engine (Apple/WhisperKit/Groq) + dán/nhập cấu hình; (6) **App Store privacy label**: Groq gửi audio lên mạng → phải khai "Audio Data"; (7) nếu bật App Sandbox: cần entitlement `com.apple.security.network.client` (client mạng) — kiểm tra `Resources/Voci.entitlements`; (8) tầng **free on-device WhisperKit** vẫn chưa code.

- [x] **WhisperKit (free tier) + router 3-engine — ĐÃ CODE (2026-07-15), CHƯA build/test trên Mac.** Giải quyết follow-up (1)(2)(5)(8) ở trên. Thêm `Sources/Speech/WhisperKitEngine.swift` (batch `SpeechEngine`, model default **`base`** ~145MB — đổi 1 chữ sang `"small"` ~480MB nếu cần chính xác Anh hơn; auto-detect ngôn ngữ; gate Apple Silicon qua `#if arch(arm64)`; state `.notReady/.preparing/.ready/.failed` + `prepare()` tải model HuggingFace lần đầu). `SpeechCapture` conform `SpeechEngine` (1 dòng). `AppState`: enum `SpeechEngineChoice` (persist `voci.speechEngine`, default `.appleOnDevice`), instances `whisper`/`groq`, `runningEngine`, `selectedEngine` (fallback về Apple khi Whisper chưa ready / máy Intel), refactor `startCapture/stopCapture/cancelCapture/confirmSave` chạy qua protocol + `handleCaptureError`, `setSpeechEngine()` kick `whisper.prepare()`. `SettingsView` thêm picker + trạng thái tải model. `project.yml` thêm package WhisperKit (`from: "0.9.0"` — có thể phải bump trên Mac). **CẦN VERIFY MAC:** API `WhisperKit(WhisperKitConfig(model:))` + `transcribe(audioPath:decodeOptions:)`/`DecodingOptions(detectLanguage:)` (đánh dấu `// UNVERIFIED` trong file — có thể khác theo version).
- [ ] **CHỐT TASK MODEL v1 (2026-07-15) — refactor VociCore + app theo `docs/task-model-v1.md`, CHƯA implement.** Quyết định (đã thảo luận + anh Khôi chốt): (a) **Option 3 "gộp hết"** — bỏ `dependsOn`, thay bằng `conditions: [Condition]` (`.taskDone(UUID)` / `.afterDate(Date)` / `.external(desc, satisfied)`), eligibility = status ∈ {todo,inProgress} AND mọi condition thỏa; (b) `deadline` vẫn là field riêng (drive ordering tier 2 + reminder, KHÔNG phải condition); (c) thêm field: `notes`, `sourceTranscript` (luôn giữ nguyên văn), `kind` (.task/.review), `estimateMinutes`, `recurrence` (daily/weekly/monthly/every-n-days, xử lý ở app layer khi done — reset in-place, engine không đọc), `reminderOverride` (nil = policy leo thang mặc định toàn cục −1d/−1h/0); (d) energy tag: bỏ. Việc cần làm: sửa `VociCore` (`Task.swift` conditions thay dependsOn; `NextTask.swift` eligibility mới cần `now` cho `.afterDate`; `DependencyGraph.swift` trích cạnh `.taskDone` từ conditions rồi DFS như cũ), cập nhật test suite §6.2 + case mới (afterDate future/past, external satisfied/unsatisfied), sửa app layer (`TaskItem` @Model mapping, `confirmSave`, `NLParser` theo checklist trong doc, UI hiển thị lý do blocked), cập nhật `specs/001-nexttask-engine/data-model.md` note superseded. Ordering 5 tier giữ nguyên. (2026-07-15)
- [ ] Chế độ "quick win theo thời gian trống" — `nextTask(from:now:availableMinutes:)` dùng `estimateMinutes` để ưu tiên task vừa khung giờ trống + cảnh báo "task cần 2h mà deadline còn 1h". Để v2, cần model v1 refactor xong trước. (2026-07-15)
- [ ] **Tầng automation ADHD (phân tích 2026-07-15, xem `docs/adhd-automation-v1.md`)** — các cơ chế đã chốt hướng, CHƯA implement, thứ tự ưu tiên trong doc (sau model v1 + NLParser Date + reminder engine): (a) **auto-unblock notify** — done một task → diff eligibility → notify task vừa hết bị chặn; (b) **resurface `afterDate`** — schedule mốc gần nhất, không polling; (c) **notification action buttons** [Xong]/[Snooze 10p]/[Mai làm] — done không mở app (`UNNotificationAction`); (d) **voice done** — "xong cái báo cáo rồi" → fuzzy match title → tick + auto-advance, dùng luôn để clear `.external`; (e) **evening sweep** — cuối ngày hỏi từng task hôm nay "xong chưa?" bằng giọng, tick hàng loạt; (f) **stale task triage** weekly — task todo quá N ngày → hỏi theo đợt "vẫn làm/chia nhỏ/dời/bỏ" (KHÔNG badge đỏ — chống shame spiral); (g) **overdue auto-reschedule** — qua deadline → notification đề nghị "tối nay/mai/cuối tuần"; (h) **`.external` quá 3 ngày** → nhắc nhẹ; (i) **breakdown thật** thay stub sample cứng trong `TaskBreakdownView` — bước con 5–15 phút chain `.taskDone` + `parentId` trỏ mẹ, bước đầu phải "dễ ngớ ngẩn", bước cuối done → auto-complete mẹ, task mẹ còn con chưa xong không được engine chọn trực tiếp; (j) **estimate-vs-elapsed** trong FocusOverlay + hỏi nhẹ khi lệch; (k) **micro-reward / gamification liều thấp, SKIN CHUYÊN NGHIỆP (định vị chốt 2026-07-15: app cho người đi làm, không trẻ con, motivate bằng momentum)** — bậc 1 + bậc 2 CHỐT LÀM: hiệu ứng done restrained kiểu Things 3/Linear (không confetti/mascot nhảy múa), giọng chữ điềm tĩnh ("6 việc hoàn thành. Ngày gọn gàng."), variable reward thể hiện bằng độ tinh tế, milestone mở theme/soundscape (thưởng phẳng không bóp méo chọn task), streak = "chuỗi ngày đều đặn" chỉ đếm chiều dương; ếch giữ ở tầng khái niệm "eat the frog" (ritual chọn việc nặng nhất ngày), bỏ tầng hoạt hình; bậc 3 full game (XP/level/quest) = KHÔNG làm; phân tích trong `docs/adhd-automation-v1.md`; (n) **Pomodoro ADHD-adapted (chốt hướng 2026-07-15)** — focus session mặc định 25' chỉnh được, hết giờ tín hiệu nhẹ không chặn mặc-định-tiếp-tục (không chặt hyperfocus), >~90' liên tục nhắc nghỉ dứt khoát; kèm quy tắc 2 phút GTD (task ≤~5' gợi ý "xử luôn"); KHÔNG làm menu chọn phương pháp/Eisenhower UI/body doubling; calendar time-blocking để v2 (nuôi quick-win mode); (o) **chế độ orchestrator — làm việc song song với AI agents (chốt hướng 2026-07-15)** — task ủy thác cho AI dùng `.external("chờ AI...", satisfied)` có sẵn trong model: "giao cho Claude rồi" → task rời menu bar + task kế trồi lên (switch là hệ quả ngầm, KHÔNG bắt tường thuật switch), timer nghiệm thu mặc định theo PHÚT ("check sau 10 phút") tự resurface dạng cần-review, done = review xong không phải prompt xong, menu bar đếm "⏳ N đang chờ AI" + WIP guardrail nhắc khẽ khi >3-4 loop mở, engine ưu tiên review-sẵn-sàng; v2 exploration — auto-detect AI xong việc: **CHỐT PRIVACY (2026-07-15) chỉ làm kiểu cooperative push** — Voci đăng ký URL scheme `voci://ai-done?cwd=$PWD` (Info.plist, không quyền mới), Claude Code Stop-hook gọi `open` URL đó; payload tối thiểu một chiều đi vào, opt-in, tín hiệu giả chỉ làm task trồi sớm (KHÔNG auto-mark-done); TỪ CHỐI mọi đường đọc lén: notification DB (Full Disk Access), Accessibility đọc nội dung app khác, ScreenCaptureKit — vi phạm constitution Principle I + phá positioning privacy-first; KHÔNG mở local HTTP server (URL scheme không có cổng lắng nghe); **setup 1 nút "Kết nối Claude Code" (chốt UX 2026-07-15)**: tự phát hiện `~/.claude/` → nút [Kết nối] → preview entry JSON → backup + MERGE append vào `hooks.Stop` của `~/.claude/settings.json` (không ghi đè hook sẵn có) → nút [Chạy thử] bắn tín hiệu xác nhận "✓ Đã nhận" → nút [Gỡ] xóa đúng entry marker `voci://`; mapping: 1 task chờ-AI → map thẳng, nhiều → match `cwd` với project folder, không chắc → ambient card một chạm; App Sandbox (MAS) không tự ghi `~/.claude/` → NSOpenPanel trỏ sẵn + security-scoped bookmark (tổng 2 click, 0 dòng lệnh), fallback "Copy hook config" dán tay; (p) **voice-reply vào terminal — HOÃN DÀI HẠN (anh Khôi 2026-07-15: MAS-first, gõ-vào-terminal là "chuyện sau này lâu lắm"; chỉ giữ chiều nhận tín hiệu agent-xong trong v2)** — agent xong → hotkey → nói → Voci gõ hộ prompt vào ĐÚNG terminal session: AppleScript `write text` nhắm theo tty (hook gửi kèm `tty=` trong URL scheme; quyền Apple Events prompt một-lần-theo-app-đích, CHỈ GHI không đọc terminal), preview transcript + đích đến BẮT BUỘC trước khi gửi (constitution II + chốt an toàn y/n prompt), fallback terminal không AppleScript = kích hoạt cửa sổ + clipboard "⌘V để gửi", KHÔNG làm `claude --resume -p` ngầm (fork hội thoại, phình scope, sandbox giết process con), caveat: Apple Events + Accessibility khó qua MAS review → cân nhắc bản Developer ID ngoài store, MAS degrade clipboard+⌘V; chi tiết `docs/adhd-automation-v1.md`;
- [ ] **Amend constitution (governance, sau khi anh Khôi duyệt — theo plan 002 Complexity Tracking):** (a) Principle I: đường cloud-parse đổi từ "AI Pro tier AND opt-in" → "mọi tier AND opt-in, free metered qua DeviceCheck" (giữ nguyên text-only/audio-never/opt-in); (b) Technology Constraints: sàn macOS 14+ với AI có điều kiện macOS 26 (thay "macOS 26+"), ASR = WhisperKit free/Groq paid (SpeechAnalyzer là tương lai), hotkey sandbox-safe (RegisterEventHotKey), recurrence vào scope v2 (danh sách v1 non-goals giữ tính lịch sử); chạy `/speckit-constitution` để bump version + sync impact report. (2026-07-15)
- [ ] **Vision v2 — định vị mới "Voice-first Workflow Command Center" + ứng viên tính năng (ĐÃ DUYỆT 2026-07-15 — anh Khôi chốt "thêm vào đi"; Tier 3 vẫn để v3; đang được specify qua Spec Kit, xem `docs/product-vision-v2.md`):** Tier 1: calendar awareness EventKit read-only (khung trống thật, deadline hiệu dụng, transition warning trước meeting), AI breakdown + parser thông minh qua Apple Foundation Models on-device macOS 26 (free/private, cloud cho paid), voice query hỏi board ("tuần này có gì?"), context resume note "save game" (voice note khi rời task → đọc lại khi quay về — rẻ, hạ tầng sẵn), meeting/voice-memo ingestion → action items hàng loạt; Tier 2: overwhelm/panic mode (ẩn hết, hiện 1 việc ≤5'), capture từ mọi nơi (Services/share extension), export/backup + search toàn văn, AI weekly narrative; Tier 3 (anh Khôi xác nhận để dành v3, 2026-07-15): iPhone/Watch companion + CloudKit sync + nhắc đa thiết bị, teamwork (collect task từ team + tự động resolve khi bên kia xong — orchestrator mở rộng sang người); ràng buộc v2: task identity ổn định + completion log dạng event cho sync sau này; KHÔNG làm: kanban/hierarchy sâu, chat AI tổng quát, social/medication. Checklist soi feature mới: xóa quyết định? chạy bằng 1 hơi thở? on-device? dùng nổi lúc tệ nhất? nuôi vòng lặp capture→decide→focus→done→review? Chờ anh Khôi duyệt tier/thứ tự. (2026-07-15) **chống drift-nagging (chốt 2026-07-15)**: recheck chờ-AI là ambient trên menu bar (KHÔNG push notification — chỉ deadline thật được làm phiền), backoff 10'→30'→gộp đợt (im lặng = đừng hỏi nữa), đối chiếu theo đợt tại điểm chạm tự nhiên (mở popover/evening sweep: "3 task giao AI lâu — cái nào xong tick luôn"), card resurface 3 nút 1 chạm [Xong rồi][Vẫn chờ][Check sau 30'] với "Vẫn chờ" tự lùi timer; chi tiết `docs/adhd-automation-v1.md` mục "Chế độ orchestrator"; (l) **"đổi gió" có cấu trúc** (chốt 2026-07-15) — lệnh giọng/nút đổi task khi chán: app đưa đúng MỘT lựa chọn thay thế (filter task hiện tại khỏi snapshot rồi gọi lại `nextTask()`, không sửa engine), task cũ giữ chỗ + re-entry hiện lại bước dở/`sourceTranscript`; timebox "10 phút thôi" với task ngán; task bị đổi-gió ≥3 lần → tự đề nghị breakdown; (m) **"Đã làm được" (accomplishment view)** — chiến tích theo phiên/ngày/tuần/tháng từ **completion log** (bảng event riêng `taskId, title, completedAt, parentId` ghi mỗi lần done — bắt buộc log riêng vì recurrence reset-in-place xóa dấu vết trên task, và `parentId` snapshot để gộp nhóm khi mẹ đã bị xóa; model thêm `completedAt` + `parentId`, xem `docs/task-model-v1.md` mục Bổ sung); **hiển thị mẹ/subtask kiểu rollup (chốt)**: phiên/ngày = chi tiết (bước con gộp nhóm dưới mẹ "✓ 3/7 bước", mẹ chưa xong vẫn hiện tiến độ; xong bước cuối → gộp 1 dòng, không đếm trùng mẹ+con), tuần/tháng = cuộn lên (chỉ việc lẻ + mẹ hoàn thành, số bước là chú thích); đếm 2 đơn vị không cộng lẫn: "việc" (task lẻ + mẹ xong) và "bước" (subtask) → "3 việc · 9 bước", đếm ếch chỉ đếm việc; guardrail: positive-only, không biểu đồ so sánh/ngày đỏ — bảng chiến tích, KHÔNG phải dashboard năng suất. (2026-07-15)
- [ ] Reminder engine (constitution IV) — scheduler đọc `deadline` + `reminderOverride`/policy mặc định, bắn `UNUserNotificationCenter`. Field đã định nghĩa trong task model v1; engine chưa có feature spec. (2026-07-15)
- [ ] **T017/T018/T026 (feature 002, Phase 3 US1) — `ParsedTask` v2 + `HeuristicNLParser` + `ParseCorrection` đã code (2026-07-16), CHƯA build/test trên Mac.** Sửa/tạo `Voci/Sources/Model/NLParser.swift` (viết lại hoàn toàn: `ParsedValue<T>`/`ParsedCondition`/`ParsedTask` v2 đúng contract `parsing-contract.md`, Codable cả 3 kiểu — thêm ngoài phần contract show ra vì `ParsedTask` bắt buộc phải Codable để router decode LLM/cloud output; `HeuristicNLParser` heuristic bilingual VI/EN: NSDataDetector cho deadline thật + fallback từ khoá thứ/ngày mai/tuần sau tiếng Việt, defer phrase → `.afterDate`, "sau khi X xong"/"after X" → `.taskDone` fuzzy-confidence, "chờ/đợi X"/"waiting for X" → `.external`, estimate có hedge-word detection (~30 phút/chắc nửa tiếng → confidence <0.7), recurrence, reminder-override, kind `.review`, `followUpReview`) và `Voci/Sources/Model/ParseCorrection.swift` (mới, `@Model` + `ParseCorrectionLog.record(attribute:parsed:corrected:transcript:in:)`). **VIỆC CÒN THIẾU (ngoài phạm vi 2 file trên, cần agent khác/anh Khôi nối):**
  - `ParseCorrection` CHƯA được đăng ký vào `Schema([VociTask.self, CompletionEvent.self])` trong `TaskStore.swift:62` → nếu không thêm `ParseCorrection.self` vào mảng đó, model mới không persist được gì (SwiftData cần mọi `@Model` type khai trong schema của container). 1 dòng, nhưng `TaskStore.swift` không thuộc 3 file được giao cho agent này nên cố ý KHÔNG sửa.
  - `HeuristicNLParser` heuristic KHÔNG tách câu ghép (compound utterance) thành nhiều task — luôn trả về đúng 1 `ParsedTask`/lần gọi (trần ≤10 task tự động thỏa vì luôn =1). Cho phép theo đúng brief T018 ("if you don't split, one task is fine") nhưng nếu sau này cần multi-task từ 1 câu nói, heuristic path cần viết thêm logic tách câu.
  - Toàn bộ call site dùng shape `ParsedTask` v1 (`title/details/when/priority/durationMinutes/context`) đang BỊ VỠ do đổi sang v2 — thuộc agent khác xử lý, liệt kê để đối chiếu: `Voci/Sources/App/AppState.swift` (`var parsed: ParsedTask?` dòng 62; `finishRecording`/`confirmSave` dòng 411-438 dùng `parsed.title/.details/.priority/.durationMinutes`); `Voci/Sources/Views/PopoverView.swift` (`parsedCard`/`contextLine`/`transcriptText` dòng ~187-249 dùng `.title/.when/.priority/.context/.durationMinutes`; preview dòng 509); `Voci/Sources/Model/SampleData.swift` (`SampleTranscript.parsed: ParsedTask` + 3 literal ở dòng 83/94/105) — file này KHÔNG thuộc danh sách loại trừ tường minh (`Sources/Parsing/**`/`PopoverView.swift`/`AppState.swift`) nhưng cũng không phải 1 trong 3 file được giao (`NLParser.swift`/`ParseCorrection.swift`) nên cố ý để nguyên, chờ agent PopoverView/AppState (T024/T025) cập nhật cùng lúc.
  - `// UNVERIFIED` trong `NLParser.swift`: (a) `NSRegularExpression`/`NSDataDetector` cache dạng `static let` trong struct có bị Swift 6 strict concurrency kêu "not concurrency-safe" không (Foundation overlay gần đây đánh dấu Sendable nhưng chưa build thật để chắc); (b) chất lượng match `NSDataDetector(.date)` với câu tiếng Việt thuần (không tiếng Anh trộn) trên macOS 14 — bảng từ khoá thứ/ngày mai/tuần sau là fallback thủ công cho đúng khoảng trống này, cần nghe thử câu thật trên Mac để tinh chỉnh confidence. (2026-07-16)

- [ ] **★★★ SUPABASE DEPLOY 2026-07-26 (project `volar` đã sống lại + `groq` proxy đã viết):**
  - [x] **Project `volar` (nuzrpipwacravfgsiacv) restore xong** → `ACTIVE_HEALTHY`. **BẪY đã gặp:** org ở plan **free = tối đa 2 project active**; restore bị `ForbiddenException` cho tới khi pause bớt. Anh Khôi chốt **pause `BuiXuanKhoi's Project` (prmykgztuiwjdvbpmagg)**. ⚠️ Nếu sau này cần project đó sống lại thì lại phải pause cái khác hoặc lên Pro $25/mo. Migration `20260716154754_parse_quota` còn nguyên sau restore (pause không mất data). (2026-07-26)
  - [x] **Edge Function `groq` đã implement** (`supabase/functions/groq/index.ts`, 303 dòng, Opus design + Sonnet execute + Opus review; `deno check` sạch). Route `POST /functions/v1/groq/audio/transcriptions` — khớp `GroqTranscriptionClient.swift` tự append `audio/transcriptions` ⇒ **KHÔNG sửa client**. Paid-only (`verifyPaidAuth`, không có nhánh free/App-Attest). Thứ tự gate: route→method→content-type→**auth trước khi đọc body**→size cap→rate limit→key. Key Groq dựng server-side, `Authorization` của client KHÔNG forward upstream. Body cap mặc định 20MB, bound thật là bộ đếm byte khi stream (chunked khai láo không lọt). Upstream 429→429, còn lại→**502 opaque** + drain body không log. Thiếu key → **503 opaque** (không leak tên biến — khác defect `missingEnv` của nhánh free `parse`). Log chỉ status/rpmCount/byte/latency. `_shared/http.ts` **chỉ thêm** `readBodyCappedBytes` (bản cũ decode UTF-8 sẽ làm hỏng audio binary) + reason `not_found` ⇒ `parse` không ảnh hưởng. (2026-07-26)
  - [ ] **ANH KHÔI SET 2 SECRET** (Dashboard → Project Settings → Edge Functions → Secrets; em không set, không cần thấy key): `GEMINI_API_KEY` → function `parse`; `GROQ_API_KEY` → function `groq`. Chưa set thì cả 2 trả 503 opaque, app tự fallback on-device (đúng thiết kế, không crash).
  - [x] **Deploy xong 2026-07-26**: `parse` v2 ACTIVE + `groq` v2 ACTIVE, cả 2 `verify_jwt=false` (đúng `config.toml` — auth thật nằm trong function). URL: `POST .../functions/v1/parse` và `POST .../functions/v1/groq/audio/transcriptions`. **Smoke test thật (Opus chạy curl):** groq không auth → `401 auth_missing`; sai route → `404 not_found` ⇒ cold start resolve được `../_shared` (layout upload đúng là kiểu CLI: `groq/index.ts` + `_shared/*.ts`, entrypoint `groq/index.ts`). ⚠️ Deploy đi qua MCP nên nội dung file phải gõ tay → agent tự thừa nhận v1 lệch comment (unicode "CHỐT"→"CHỮT", 1 comment gemini.ts bị cắt), đã redeploy v2 sinh từ file thật. Comment trong bundle đã deploy có thể còn lệch cosmetic so với repo; lần deploy sau bằng CLI sẽ ghi đè từ đĩa. (2026-07-26)
  - [ ] **⚠️ `parse` VALIDATE BODY TRƯỚC KHI AUTH (Opus phát hiện qua smoke test 2026-07-26, CHƯA sửa):** gọi `POST /functions/v1/parse` với `{}` và **không có auth** → trả `400 invalid_request` kèm `detail: "transcript is required and must be a non-empty string"`. Đọc code: `readBodyCapped` (L71) → `validateRequestBody` (L84-86) → **rồi mới** auth (L91-100). Hệ quả: (a) caller chưa auth dò được schema/validation rule; (b) traffic chưa auth vẫn chạy hết đường parse JSON + validate. Không rò key, và invocation vẫn bị tính tiền dù reorder hay không, nên severity THẤP-TRUNG BÌNH. **Cách sửa (đã xác định, giữ đúng ràng buộc):** phải GIỮ `readBodyCapped` trước auth vì nhánh free `verifyFreeAuth(deviceTokenHeader, rawBody)` cần `rawBody` để bind App Attest clientDataHash; chỉ **chuyển `validateRequestBody` xuống SAU auth**. Function `groq` (viết sau) đã auth-trước-body nên không bị. Sửa xong phải redeploy — chờ có CLI để deploy rẻ. (2026-07-26)
  - [ ] **Tách bucket rate-limit cho `groq`** (agent tìm ra, Opus ghi nhận chưa sửa): `groq` và `parse` dùng CHUNG bảng `parse_rate_limit` + cùng key `SHA-256(bundleId:originalTransactionId)` + cùng biến `PARSE_PAID_RPM` ⇒ subscriber transcribe dồn dập có thể tự làm mình 429 khi parse task, và 2 route không tune độc lập được. Soft abuse bound nên không phải bug đúng/sai. Cách sửa: prefix key riêng (`"groq:" + keyHash`) hoặc bảng/RPC `groq_rate_limit`. Làm trước khi có traffic thật. (2026-07-26)
  - [ ] **Cài Supabase CLI + login để deploy bằng CLI** (rule mới trong CLAUDE.md global 2026-07-26: deploy function thì DÙNG CLI, không dùng MCP `deploy_edge_function` vì MCP bắt nhồi toàn bộ script vào tham số → tốn token + chậm). Hiện máy **chưa có** CLI (`supabase: command not found`) và không có `SUPABASE_ACCESS_TOKEN`; `supabase login` là interactive nên anh Khôi phải tự chạy (`! npx supabase login` hoặc cài qua scoop/npm). Đợt deploy 2026-07-26 vẫn phải đi qua MCP vì thiếu CLI. (2026-07-26)

- [ ] **CHỐT MÔ HÌNH FREEMIUM + BACKEND (2026-07-15, Option 1 "gọn"):** free = **WhisperKit on-device KHÔNG giới hạn** (0 chi phí server → không cần đếm limit); Groq (paid cloud) mở **sau khi mua qua StoreKit**. **KHÔNG bắt tạo account.** Gate paid = **StoreKit `Transaction` JWS** verify server-side (restore across máy tự động qua Apple ID — không cần hệ account riêng). **CẬP NHẬT 2026-07-15 (feature 002): DeviceCheck GIỜ CẦN LÀM** — cloud LLM parse-to-task mở FREE cho mọi tier (anh Khôi: paid-only theo phần cứng là phân biệt) với quota 50 parse/ngày/thiết bị đếm bằng DeviceCheck + trần 10 task/lần nói; proxy nhận 2 kiểu auth (JWS paid unmetered / DeviceCheck free metered); vượt quota rơi về heuristic parser, không chặn. Groq speech vẫn paid-only như cũ. **Ma trận freemium v2 chốt cấu trúc (2026-07-15, xem `docs/product-vision-v2.md` mục Freemium v2):** nguyên tắc "tính năng free hết, chỉ cloud compute trả tiền" — Free: mọi feature + WhisperKit unlimited + FM on-device unlimited + cloud parse 50/ngày/thiết bị + heuristic; Pro: Groq speech + cloud parse/breakdown unlimited + meeting ingestion Groq + narrative cloud. **TÁI CẤU TRÚC CONVERSION đã duyệt (2026-07-15)**: trial 7-14 ngày full Pro (StoreKit intro offer) → rơi về free; quota AI free hạ ~20-25/ngày + HIỆN đếm; chuyển sang Pro: meeting ingestion (Pro-only), AI weekly narrative, history >30 ngày + view tháng (export vẫn free), theme pack; juice/micro-reward free cho cả 2 tier (chỉ bán content pack). **GIÁ ĐỀ XUẤT (phân tích chi phí trong `docs/product-vision-v2.md`, đơn giá verify 2026-07: Groq $0.111/h large-v3 · $0.04/h turbo min-10s, Gemini 3.1 Flash-Lite $0.25/$1.50 per 1M)**: Pro $4.99/mo · $39.99/yr · VN storefront 99k/799k · trial 14 ngày · không lifetime lúc launch — **ANH KHÔI ĐÃ DUYỆT SỐ CUỐI (2026-07-15)**, kèm thang nâng giá theo mốc cho user mới (Release B → $5.99/$49.99, v3 sync → $7.99/$59.99+, user cũ grandfather) ghi trong `docs/product-vision-v2.md`; chi phí biến đổi Pro điển hình ~$0.8-1/mo (nặng nhất ~$1.8 với fair-use cap: ingestion 20h/tháng, parse 100/ngày) → margin ~78% monthly / ~67% yearly sau Apple 15%; break-even ~8-12 subscribers; free user ~$0.05-0.4/mo kiểm soát bằng quota server-side + 2.5 Flash-Lite + turbo cho file dài. CHỜ anh Khôi duyệt SỐ GIÁ CUỐI. **Proxy = Supabase Edge Function** (Deno): giữ **Groq key trong Supabase Secrets** (`Deno.env.get`), verify StoreKit JWS + entitlement (Postgres/RLS) + rate-limit, forward sang Groq `/audio/transcriptions`, trả transcript. Client production: `GroqCredentialProvider` trả `baseURL=https://<proj>.supabase.co/functions/v1/groq` + `Authorization: Bearer <StoreKit JWS/token từ Keychain>` (client append `audio/transcriptions` → function route nội bộ, KHÔNG sửa `GroqTranscriptionClient`). **LÝ DO proxy bắt buộc:** key ở client bị moi 100% (strings binary / MITM Proxyman đọc header Bearer / dump memory) → dùng key/quota của anh, có thể bị Groq khoá tài khoản. CHƯA làm: (a) dựng Supabase project + Edge Function `groq`; (b) StoreKit config + verify với App Store Server API (cần App Store Connect API key); (c) client production provider đọc token Keychain; (d) App Attest chống script giả danh app (tuỳ chọn); (e) App Store privacy label "Audio Data" cho nhánh Groq.
